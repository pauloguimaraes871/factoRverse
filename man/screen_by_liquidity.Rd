% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screen_by_liquidity.R
\name{screen_by_liquidity}
\alias{screen_by_liquidity}
\alias{screen_by_liquidity,meta_dataframe,meta_dataframe,data.frame,character-method}
\title{Generic function for screening a meta_dataframe based on liquidity classification, given a liquidity_floor_rule.}
\usage{
screen_by_liquidity(
  meta_dataframe,
  liquidity_m_df,
  liquidity_floor_cutoffs,
  liquidity_floor_rule,
  ...
)

\S4method{screen_by_liquidity}{meta_dataframe,meta_dataframe,data.frame,character}(
  meta_dataframe,
  liquidity_m_df,
  liquidity_floor_cutoffs,
  liquidity_floor_rule,
  verbose = TRUE
)
}
\arguments{
\item{meta_dataframe}{A \code{meta_dataframe} object.}

\item{liquidity_m_df}{A \code{liquidity_m_df} meta_dataframe containing one or more market liquidity measures (e.g., inflation-adjusted mean financial volume).
All ids in meta_dataframe must have a unique correspondence to this object.}

\item{liquidity_floor_cutoffs}{A data.frame containing cutoff values for liquidity metrics specified in \code{liquidity_m_df}.
The names should match the metrics and values should be the minimum acceptable values (adjust for inflation)
Stocks that have all metrics higher than defined in a \code{liquidity_floor_cutoffs} element will receive a liquidity classification at least equal to it.
Elements should be: "micro_caps", "small_caps", "mid_caps", "large_caps" and "mega_caps"
Classification should be in ascending order (from lest liquid to most liquid) for all metrics.
If set in decimals, values will be interpreted as quantiles and classification will be set according to quantiles
The first column is named "liquidity_classification"
It has at most 5 rows and no duplicates or NAs.}

\item{liquidity_floor_rule}{Optional. Character string specifying the liquidity classification to apply the liquidity floor rule (eg. "nano_caps", "micro_caps", "small_caps", "mid_caps", "large_caps", "mega_caps").}

\item{...}{Additional arguments to be passed to the method.}

\item{verbose}{Logical. If TRUE, prints additional information during processing.}
}
\value{
A new \code{meta_dataframe} containing only the ids whose liquidity classification meets or exceeds
\code{liquidity_floor_rule} (via \code{classify_stock_liquidity()}), with a screening step appended to the
\code{workflow} log. Errors if every stock is filtered out.
}
\description{
This function filters a \code{meta_dataframe} using \code{classify_stock_liquidity()} to remove illiquid stocks.
}
\examples{
\dontrun{
# Drop everything less liquid than small caps
screen_by_liquidity(features_m_df, liquidity_m_df, liquidity_floor_cutoffs,
                    liquidity_floor_rule = "small_caps")
}

}
