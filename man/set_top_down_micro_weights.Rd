% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-create_mmaf_portfolio.R
\name{set_top_down_micro_weights}
\alias{set_top_down_micro_weights}
\title{Set Top-Down Micro Weights}
\usage{
set_top_down_micro_weights(
  group_name,
  group_weights = NULL,
  micro_port_construction_method,
  group_members,
  universe_m_d_ref,
  liquidity_m_d_ref = NULL,
  eligible_returns_m_xts_upd_ref = NULL,
  selected_benchmark_m_xts_upd_ref = NULL,
  active_returns = if (is.null(selected_benchmark_m_xts_upd_ref)) FALSE else TRUE,
  cov_estimation_method = "sample",
  cov_matrix_sample_size = if (is.null(eligible_returns_m_xts_upd_ref)) NULL else
    nrow(eligible_returns_m_xts_upd_ref),
  groups_m_d_ref = NULL,
  selected_benchmark = NULL,
  bench_assets_returns_m_xts_upd_ref = NULL,
  concentration_constraint_policy = NULL,
  turnover_constraint_policy = NULL,
  liquidity_constraint_policy = NULL,
  cap_weighting_metric = NULL,
  rp_method = "cyclical-spinu",
  exp_ret_score_tilt = NULL,
  exp_ret_score_tilt_eta = NULL,
  linkage = "single",
  n_random_ports = 2000,
  random_ports_method = "sample",
  opt_objective = "sharpe",
  opt_method = "random",
  ridge_pen = NULL,
  n_resamples = 0,
  exp_ret_score_jitter = 0,
  cov_eigval_jitter = 0,
  lower_quantile_winsorization = 0.025,
  upper_quantile_winsorization = 0.975
)
}
\arguments{
\item{group_name}{Character. Name of the group being processed.}

\item{group_weights}{Named numeric vector of group weights. Required if
any constraint or ridge penalty is defined.}

\item{micro_port_construction_method}{Character. Method used for portfolio
construction within the group (e.g., \code{"mvo"}, \code{"risk_parity"}).}

\item{group_members}{List mapping group names to member tickers.}

\item{universe_m_d_ref}{Data frame with stock-level reference data, including
tickers and optional benchmark/target weights.}

\item{liquidity_m_d_ref}{(Optional) Data frame with liquidity metrics for
the full universe.}

\item{eligible_returns_m_xts_upd_ref}{An optional \code{xts} object containing return data for the eligible tickers, used in covariance matrix estimation for Risk-Parity and MVO methods.}

\item{selected_benchmark_m_xts_upd_ref}{An optional \code{xts} object containing benchmark returns used to compute active returns (only if \code{active_returns = TRUE}).}

\item{active_returns}{Logical. If \code{TRUE}, covariance estimation will use active returns (asset returns minus benchmark). Defaults to \code{FALSE} if \code{selected_benchmark_m_xts_upd_ref} is \code{NULL}, otherwise \code{TRUE}.}

\item{cov_estimation_method}{An optional character string specifying the method for estimating the covariance matrix. Defaults to \code{NULL}.}

\item{cov_matrix_sample_size}{Integer. Number of time periods (rows in \code{eligible_returns_m_xts_upd_ref}) used to estimate the covariance matrix. If \code{NULL}, uses all available observations.}

\item{groups_m_d_ref}{An optional data frame used for group constraints and covariance matrix estimation. Should include group information if used. Defaults to \code{NULL}.}

\item{selected_benchmark}{It controls whether a 'port' object for the benchmark should be created and added to portolio results.}

\item{bench_assets_returns_m_xts_upd_ref}{Optional. A 'xts' object containing returns for all stocks. Needed for computing benchmark and port stats.}

\item{concentration_constraint_policy}{(Optional) Policy object controlling
max active weights at stock level.}

\item{turnover_constraint_policy}{(Optional) Policy object controlling stock
or group-level turnover caps.}

\item{liquidity_constraint_policy}{(Optional) Policy object controlling stock
or group-level liquidity caps.}

\item{cap_weighting_metric}{(Optional) Market cap or related metric for
cap-weighted allocations.}

\item{rp_method}{(Optional) Risk parity method.}

\item{exp_ret_score_tilt}{(Optional) Expected return tilt applied under risk parity.}

\item{exp_ret_score_tilt_eta}{(Optional) Tilt intensity for expected return tilt.}

\item{linkage}{(Optional) Linkage method for hierarchical clustering in risk parity.}

\item{n_random_ports}{(Optional) Number of random portfolios used for resampling.}

\item{random_ports_method}{(Optional) Sampling method for random portfolios.}

\item{opt_objective}{(Optional) Optimization objective (e.g., \code{"min_var"}, \code{"sharpe"}).}

\item{opt_method}{(Optional) Numerical optimization routine.}

\item{ridge_pen}{(Optional) Ridge penalty term for regularized optimization.}

\item{n_resamples}{(Optional) Number of resamples used in MVO.}

\item{exp_ret_score_jitter}{(Optional) Jitter applied to expected return scores.}

\item{cov_eigval_jitter}{(Optional) Jitter applied to covariance eigenvalues.}

\item{lower_quantile_winsorization}{(Optional) Lower quantile cutoff.}

\item{upper_quantile_winsorization}{(Optional) Upper quantile cutoff.}
}
\value{
A portfolio object returned by \code{set_portfolio_weights()} with weights
for tickers in the specified group.
}
\description{
Constructs a micro-level portfolio within a given group as part of a
hierarchical risk parity allocation. The function subsets the universe,
covariance matrix, and (optionally) liquidity data for the group, scales
constraints relative to group weights, and calls
\code{set_portfolio_weights()} to compute intra-group allocations.
}
\details{
\itemize{
\item Scales benchmark, BOP, and target weights by the group allocation if constraints are active.
\item Normalizes scaled weights to sum to 1 if needed.
\item Scales concentration, turnover, and liquidity constraints relative to
the group weight.
\item Removes pre-existing weight columns if no constraints are active.
}
}
\seealso{
\code{\link[=process_micro_portfolios]{process_micro_portfolios()}}, \code{\link[=set_portfolio_weights]{set_portfolio_weights()}}
}
\keyword{optimization}
\keyword{portfolio}
\keyword{risk-parity}
