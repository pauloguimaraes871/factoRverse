% Generated by roxygen2: do not edit by hand
% Please edit documentation in
%   R/deprecated-adjust_ml_walk_forward_validation_results_list.R
\name{adjust_ml_walk_forward_validation_results_list}
\alias{adjust_ml_walk_forward_validation_results_list}
\title{Adjust Machine Learning Walk-Forward Validation Results (Deprecated)}
\usage{
adjust_ml_walk_forward_validation_results_list(
  ml_walk_forward_validation_results_list,
  current_date
)
}
\arguments{
\item{ml_walk_forward_validation_results_list}{A list where each element is a list containing
\code{prediction_list} and \code{metadata}. \code{prediction_list} should be a named list of predictions, and
\code{metadata} should include the machine learning algorithm used.}

\item{current_date}{A string representing the date for which predictions should be extracted.}
}
\value{
A data frame where each row corresponds to a ticker, and columns correspond to predictions
from different machine learning algorithms. The column names are the machine learning algorithms
used. A warning is issued if any column shows no variation in its values.
}
\description{
\strong{Deprecated}: This function is deprecated and will be removed in a future release. Please use
}
\details{
This function processes a list of machine learning walk-forward validation results and extracts
the predictions for a specified date. It combines these predictions into a single data frame and
renames the columns based on the machine learning algorithms used. The function also warns if any
of the columns in the resulting data frame show no variation in their values.

This function assumes that \code{ml_walk_forward_validation_results_list} is a list of lists,
each containing a \code{prediction_list} (which is a list of predictions keyed by date) and \code{metadata}
(which includes information such as the machine learning algorithm used). It uses the specified
\code{current_date} to extract predictions and merge them into a single data frame. Columns are named
according to the machine learning algorithms used. If any column has no variation (i.e., all values
are identical), a warning is generated.
}
