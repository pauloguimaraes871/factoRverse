% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-derive_stock_universe_m_d_ref.R
\name{derive_stock_universe_m_d_ref}
\alias{derive_stock_universe_m_d_ref}
\title{Build Stock Universe with Expected Return Score}
\usage{
derive_stock_universe_m_d_ref(
  signals_m_d_ref,
  oos_predictions_m_d_ref = NULL,
  chosen_score_metric_and_position = NULL,
  scaler_m_d_ref = NULL,
  chosen_scaler = NULL,
  scaler_shrinkage = 0,
  lower_quantile_winsorization,
  upper_quantile_winsorization
)
}
\arguments{
\item{signals_m_d_ref}{A data frame containing at least the columns \code{id}, \code{tickers},
and one or more signal metrics.}

\item{oos_predictions_m_d_ref}{Optional. A data frame with out-of-sample predictions.
It must contain columns \code{id} and \code{pred}. If not provided (i.e. \code{NULL}), the signal
from \code{signals_m_d_ref} is used.}

\item{chosen_score_metric_and_position}{A named character vector indicating the chosen
signal metric and its associated position. For example, \code{c("signal" = "long")}
implies a long position (multiplier 1) while \code{c("signal" = "short")} implies
a short position (multiplier -1).}

\item{scaler_m_d_ref}{Optional. A data frame containing scaling factors with columns \code{id}, \code{tickers},
\code{dates}, and one or more scaler metrics. If not provided (i.e. \code{NULL}), no scaling is applied.}

\item{chosen_scaler}{Optional. A string indicating the chosen scaler metric from \code{scaler_m_d_ref}.
If not provided (i.e. \code{NULL}), no scaling is applied.}

\item{scaler_shrinkage}{Numeric between 0 and 1. If greater than 0, applies shrinkage to the scaler values
towards 1 (i.e., no scaling). A value of 0 means no shrinkage, while a value of 1 means all scalers are set to 1.
Default is 0.}

\item{lower_quantile_winsorization}{Numeric. Lower quantile value for winsorization
in \code{signal_transform}.}

\item{upper_quantile_winsorization}{Numeric. Upper quantile value for winsorization
in \code{signal_transform}.}
}
\value{
A data frame with columns:
\item{\code{id}}{A unique identifier combining the ticker and date.}
\item{\code{tickers}}{Ticker symbols.}
\item{\code{dates}}{The current date.}
\item{\code{exp_ret_score_raw}}{The transformed (winsorized/z-scored and sign-adjusted) score before any scaling.}
\item{\code{scaler}}{The winsorized and (optionally) shrunk scaling factor. Present only when \code{scaler_m_d_ref} and \code{chosen_scaler} are supplied.}
\item{\code{exp_ret_score}}{The final expected return score - \code{exp_ret_score_raw} multiplied by \code{scaler} when scaling, otherwise equal to \code{exp_ret_score_raw}. Always the last column.}
}
\description{
This function creates a stock universe data frame by merging ticker information
with either out-of-sample predictions or signal values. It then transforms the score
using a user-provided function (\code{signal_transform}), which is typically used to
winsorize, z-score, and/or otherwise adjust the raw signal.
}
\examples{
signals_m_d_ref <- data.frame(
  id = paste0(c("AAA", "BBB", "CCC"), "-2020-01-31"),
  tickers = c("AAA", "BBB", "CCC"),
  dates = as.Date("2020-01-31"),
  book_yield = c(0.08, 0.05, 0.12)
)
# Long tilt on a book-yield characteristic
derive_stock_universe_m_d_ref(
  signals_m_d_ref = signals_m_d_ref,
  chosen_score_metric_and_position = c(book_yield = "long"),
  lower_quantile_winsorization = 0.05,
  upper_quantile_winsorization = 0.95
)

}
