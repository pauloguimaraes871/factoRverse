% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_bank_characteristics_na.R
\name{set_bank_characteristics_na}
\alias{set_bank_characteristics_na}
\alias{set_bank_characteristics_na,meta_dataframe,character-method}
\title{Set Non-Meaningful Characteristics to NA for Financial Firms}
\usage{
set_bank_characteristics_na(
  meta_dataframe,
  sector_column,
  bank_values = c("Bank", "Financial", "Banking"),
  insurance_values = c("Insurance", "Insurer"),
  characteristics = NULL
)

\S4method{set_bank_characteristics_na}{meta_dataframe,character}(
  meta_dataframe,
  sector_column,
  bank_values = c("Bank", "Financial", "Banking"),
  insurance_values = c("Insurance", "Insurer"),
  characteristics = NULL
)
}
\arguments{
\item{meta_dataframe}{A \code{meta_dataframe} object containing firm characteristics.}

\item{sector_column}{A \code{character} specifying the column name that identifies
sector classification (e.g., "sectors_to_treat_as_banks", "sectors_dynamic").}

\item{bank_values}{A \code{character} vector of values in \code{sector_column} that
identify banks. Default is \code{c("Bank", "Financial", "Banking")}.}

\item{insurance_values}{A \code{character} vector of values in \code{sector_column} that
identify insurance companies. Default is \code{c("Insurance", "Insurer")}.}

\item{characteristics}{A \code{character} vector of characteristic names to set to NA
for financial firms. If NULL (default), uses a standard list based on asset
pricing literature.}
}
\value{
A modified \code{meta_dataframe} object with specified characteristics set to NA
for financial firms.
}
\description{
Sets specific financial characteristics to NA for banks and insurance companies,
following standard practices in asset pricing research. Certain characteristics
that are meaningful for manufacturing and retail firms (e.g., inventory, COGS,
PPE, capex) are not applicable to financial institutions.
}
\details{
The function identifies firms classified as banks or insurance companies based on
the \code{sector_column} and sets non-applicable characteristics to NA. This follows
the standard practice in asset pricing research (e.g., Fama-French methodology)
where certain characteristics are excluded for financial firms.

\strong{Default characteristics set to NA for banks and insurers:}
\itemize{
\item \strong{Inventory-related}: \code{inventory}, \code{st_inventory}
\item \strong{Non-bank COGS}: \code{cogs_nonbanks_3m}, \code{cogs_nonbanks_12m}
\item \strong{Non-bank Sales}: \code{sales_nonbanks_3m}, \code{sales_nonbanks_12m}
\item \strong{Capital Expenditures}: \code{capex_int_12m}, \code{capex_int_3m}, \code{capex_ppe_12m}, \code{capex_ppe_3m}
\item \strong{Property, Plant & Equipment}: \code{ppe}
\item \strong{SG&A}: \code{sga_12m}, \code{sga_3m}
\item \strong{Depreciation}: \code{depreciation_12m}, \code{depreciation_3m}
}

\strong{Characteristics that remain meaningful for banks:}
\itemize{
\item Bank-specific: \code{cogs_banks_*}, \code{sales_banks_*}, \code{newloans_*}, \code{int_inc_*}, \code{serv_rev_*}
\item Universal metrics: assets, book equity, cash, debt, prices, returns, volatility, beta
}
}
\examples{
\dontrun{
# Create sample data
features_m_df <- create_meta_dataframe(...)

# Set non-meaningful characteristics to NA for banks
features_m_df <- set_bank_characteristics_na(
  features_m_df,
  sector_column = "sectors_to_treat_as_banks",
  bank_values = c("Bank", "Financial"),
  insurance_values = c("Insurance")
)

# Custom characteristics list
features_m_df <- set_bank_characteristics_na(
  features_m_df,
  sector_column = "sectors_dynamic",
  characteristics = c("inventory", "ppe", "capex_int_12m")
)
}

}
\references{
Fama, E. F., & French, K. R. (1992). The cross-section of expected stock returns.
The Journal of Finance, 47(2), 427-465.

Fama, E. F., & French, K. R. (1993). Common risk factors in the returns on stocks
and bonds. Journal of Financial Economics, 33(1), 3-56.
}
