% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_window.R
\name{compute_window}
\alias{compute_window}
\alias{compute_window,meta_dataframe,numeric,character-method}
\alias{compute_window,meta_xts,numeric,character-method}
\title{Compute Rolling or Seasonal Calculations for a Given Metric in meta_dataframe or meta_xts}
\usage{
compute_window(data, period, FUN, ...)

\S4method{compute_window}{meta_dataframe,numeric,character}(
  data,
  period,
  FUN,
  window = "rolling",
  signal,
  benchmark_returns_m_xts = NULL,
  selected_bench = NULL,
  na.rm = TRUE,
  only_unique = FALSE,
  feature_name = NULL,
  min_non_na = 0,
  count_condition_fun = NULL,
  offset_months = 0
)

\S4method{compute_window}{meta_xts,numeric,character}(
  data,
  period,
  FUN,
  window = "rolling",
  col_name,
  benchmark_returns_m_xts = NULL,
  selected_bench = NULL,
  na.rm = TRUE,
  only_unique = FALSE,
  feature_name = NULL,
  min_non_na = 0,
  specific_dates = NULL,
  mult_last_n = 0,
  mult_by = if (FUN == "geom_mean_ret") 0 else -1,
  top_n = 1
)
}
\arguments{
\item{data}{A \code{meta_dataframe} or \code{meta_xts} object.}

\item{period}{A \code{numeric} value indicating the time window:}

\item{FUN}{A \code{character} specifying the function to apply. Supported options depend on the input class:
\itemize{
\item For a \code{meta_dataframe}: "sum", "mean", "geom_mean_ret", "median", "sd", "skew", "sur", "cagr",
"signal_to_noise", "res_mom", "idio_vol", "count_if", "max", "min", "lag".
\item For a \code{meta_xts}: "mean", "median", "geom_mean_ret", "max", "min", "sd", "skew", "sur", "cagr",
"signal_to_noise", "alpha", "alpha_tstat", "beta", "correlation", "res_mom", "idio_vol".
The benchmark-based FUNs ("res_mom", "idio_vol", "alpha", "alpha_tstat", "beta", "correlation") require
\code{benchmark_returns_m_xts} and \code{selected_bench}.
}}

\item{...}{Additional arguments passed to the function.}

\item{window}{A \code{character} specifying the window type: either "rolling" (default) or "seasonal".}

\item{signal}{(For \code{meta_dataframe}) A \code{character} specifying the column name on which the rolling function is computed.}

\item{benchmark_returns_m_xts}{A \code{meta_xts} object. Required for \code{FUN} "res_mom" and "idio_vol".}

\item{selected_bench}{A \code{character} specifying the column name in \code{benchmark_returns_m_xts@data} to use. Required for \code{FUN} "res_mom" and "idio_vol".}

\item{na.rm}{A \code{logical} indicating whether to remove \code{NA} values (default: TRUE).}

\item{only_unique}{A \code{logical} indicating whether to compute the metric using only unique values (default: FALSE).}

\item{feature_name}{A \code{character} specifying the name of the computed feature. If NULL (default), the feature name is set to \code{"<metric>_<window>_<period>_<FUN>"}.}

\item{min_non_na}{A \code{numeric} value specifying the minimum number of non-NA values required to compute the rolling statistic. Default is 0.
\itemize{
\item Exception: For \code{FUN = "cagr"}, the default is \code{period + 1} to ensure sufficient periods.
}}

\item{count_condition_fun}{A function that takes a numeric vector and returns a logical vector.
The function should return \code{TRUE} for elements that should be counted. Only used for FUN = "count_if".}

\item{offset_months}{A \code{numeric} value specifying the number of months to offset ahead or backwards for \code{seasonal} window type.}

\item{col_name}{(For \code{meta_xts}) A \code{character} specifying the column name.}

\item{specific_dates}{A \code{Date} vector specifying specific dates to consider for the rolling calculation.}

\item{mult_last_n}{A \code{numeric} value indicating the number of most recent observations to multiply by a factor when computing certain metrics (default: 0).}

\item{mult_by}{A \code{numeric} value indicating the multiplication factor for the last \code{n} observations when computing certain metrics (default: -1 for most metrics, 0 for "geom_mean_ret").}

\item{top_n}{A \code{numeric} value indicating the number of top elements to consider when computing the "max" function (default: 1).}
}
\value{
A modified \code{meta_dataframe} or \code{meta_xts} object with an additional column named \code{"<metric>_<window>_<period>_<FUN>"},
storing the computed values.
}
\description{
This method computes rolling or seasonal statistics for a specified metric in a \code{meta_dataframe} or \code{meta_xts} object.
The function applies a predefined calculation (\code{FUN}) to values within a specified time window.
}
\details{
The window can be either:
\itemize{
\item \strong{Rolling:} Includes all dates within the range \verb{[current_date - period months, current_date]}.
\item \strong{Fwd Seasonal:} Includes only observations from the same ticker whose months match the consecutive months immediately following the current observation's month.
}

The function filters observations within the specified time window and applies the selected \code{FUN}. If no matching observations are found, the result is \code{NA}.

Available functions:
\itemize{
\item \strong{sum}: \code{sum(x, na.rm = na.rm)} (meta_dataframe only).
\item \strong{mean}: \code{mean(x, na.rm = na.rm)}.
\item \strong{geom_mean_ret}: Geometric mean return over the window (percentage-point scale).
\item \strong{median}: \code{stats::median(x, na.rm = na.rm)}.
\item \strong{sd}: \code{stats::sd(x, na.rm = na.rm)}.
\item \strong{skew}: Bias-adjusted skewness of the values in the window.
\item \strong{sur}: \code{(final_value - mean(x, na.rm = na.rm)) / stats::sd(x, na.rm = na.rm)}, where \code{final_value} is the most recent value.
\item \strong{cagr}: Compound growth rate between the first and last value of the window.
\item \strong{signal_to_noise}: \code{mean(x) / sd(x)} (mean-to-standard-deviation ratio).
\item \strong{res_mom}: Rolling regression of the metric on the benchmark; returns the standardized sum of residuals (residual momentum).
\item \strong{idio_vol}: Rolling regression against the benchmark; returns \code{sqrt(sd(metric)^2 - beta^2 * sd(benchmark)^2)}.
\item \strong{alpha}, \strong{alpha_tstat}, \strong{beta}, \strong{correlation}: Rolling CAPM-style statistics against the benchmark (meta_xts only).
\item \strong{count_if}: Counts the number of elements that satisfy \code{count_condition_fun} (meta_dataframe only).
\item \strong{max}: \code{max(x, na.rm = na.rm)} for a meta_dataframe; sum of the top \code{top_n} values for a meta_xts.
\item \strong{min}: \code{min(x, na.rm = na.rm)}.
\item \strong{lag}: Retrieves the observation that happened \code{period} months ago (meta_dataframe only).
}
}
