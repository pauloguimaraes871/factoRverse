% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-fit_bayesian_model.R
\name{fit_bayesian_model}
\alias{fit_bayesian_model}
\title{Fit a Bayesian Hierarchical Regression Model and Summarize Posterior Metrics}
\usage{
fit_bayesian_model(
  signal_universe_m_d_ref,
  selected_backtest_returns_corrected_positions_upd_ref,
  selected_market_factor_proxy_vector_upd_ref,
  elected_priors,
  signal_themes_m_d_ref,
  model_spec_theme_level,
  chains,
  iter,
  warmup,
  thin,
  seed,
  adapt_delta,
  verbose,
  parallel
)
}
\arguments{
\item{signal_universe_m_d_ref}{A data frame containing the signal universe. This data frame should include:
\itemize{
\item \code{tickers}: Unique identifiers for signals.
\item \code{is_eligible}: A logical column indicating if a signal is eligible for modeling.
\item \code{final_signal}: Finalized signals to be used for modeling.
}}

\item{selected_backtest_returns_corrected_positions_upd_ref}{A data frame containing the backtest returns data for various signals.
\itemize{
\item The first column should include dates.
\item Remaining columns represent signals (e.g., tickers) and their respective active returns.
}}

\item{selected_market_factor_proxy_vector_upd_ref}{A numeric vector containing benchmark returns data. The vector will be recycled
to match the length of the backtest returns data.}

\item{elected_priors}{An object of class \code{brmsprior} containing priors for the Bayesian model, such as those generated by the
\code{\link{get_priors_from_informative_data}} function or provided directly by the user.}

\item{signal_themes_m_d_ref}{A data frame containing metadata about signals. This data frame should include:
\itemize{
\item \code{tickers}: Signal identifiers matching those in \code{selected_backtest_returns_corrected_positions_upd_ref}.
\item \code{theme}: Group membership for each signal, defining the clusters for the Bayesian hierarchical model.
\item \code{dates}: Dates corresponding to the backtest data.
This input ensures proper alignment between signals and their associated themes.
}}

\item{model_spec_theme_level}{A character string specifying the desired Bayesian model structure.
Options include:
\itemize{
\item \code{"random_intercept"}: Includes random effects for the intercept at the theme level.
\item \code{"fixed_intercepts"}: Uses fixed intercepts for each theme.
\item \code{"fixed_intercepts_and_slopes"}: Includes fixed intercepts and slopes for each theme.
\item \code{"none"}: Omits theme-level intercepts but includes random effects at the theme:signal level.
}}

\item{verbose}{A logical indicating whether to display progress messages during the model fitting process. Defaults to \code{TRUE}.}

\item{parallel}{A logical indicating whether to enable parallel computation using \code{future}. Defaults to \code{TRUE}.}
}
\value{
A named list containing:
\itemize{
\item \code{bayesian_model}: A \code{brmsfit} object containing the fitted Bayesian model. This object includes posterior distributions,
parameter estimates, diagnostics, and other details of the model fit.
\item \code{posterior_signal_universe_m_d_ref}: A data frame containing the updated \code{signal_universe_m_d_ref} with posterior summary statistics.
}
}
\description{
This function fits a Bayesian hierarchical regression model using the \code{brms} package and computes posterior summary
statistics for signal-level metrics. The model captures signal clustering within themes and provides detailed
posterior metrics for use in performance analysis.
}
\details{
The function performs the following steps:
\enumerate{
\item \strong{Preprocessing}: Converts the backtest returns data into long format, merges metadata about signals and themes,
and adds the market factor proxy.
\item \strong{Formula Specification}: Defines the Bayesian model formula based on the chosen \code{model_spec_theme_level}.
\item \strong{Model Fitting}: Fits the Bayesian model using the \code{brm} function from the \code{brms} package, incorporating the
provided priors and prepared data.
\item \strong{Posterior Summarization}: Extracts posterior draws and computes signal-level metrics such as alphas, betas,
sigmas, active returns, tracking errors, information ratios (IR), appraisal ratios (AP), and Treynor ratios.
Updates the \code{signal_universe_m_d_ref} data frame with these metrics.
}

This hierarchical approach allows the model to capture both global (theme-level) and local (signal-level) effects
while leveraging the flexibility of Bayesian regression. The inclusion of posterior metrics enables deeper
performance analysis of signals.
}
\examples{
\dontrun{
# Example usage
results <- fit_bayesian_model(
  signal_universe_m_d_ref = signal_universe_data,
  selected_backtest_returns_corrected_positions_upd_ref = backtest_data,
  selected_market_factor_proxy_vector_upd_ref = benchmark_data,
  elected_priors = priors_list,
  signal_themes_m_d_ref = signal_metadata,
  model_spec_theme_level = "random_intercept",
  verbose = TRUE,
  parallel = TRUE
)

# Access the Bayesian model and posterior metrics
bayesian_model <- results$bayesian_model
posterior_metrics <- results$posterior_signal_universe_m_d_ref
}

}
