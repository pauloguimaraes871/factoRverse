% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-create_hrp_portfolio.R
\name{create_hrp_portfolio}
\alias{create_hrp_portfolio}
\title{Create a Hierarchical Risk Parity (HRP) portfolio}
\usage{
create_hrp_portfolio(
  universe_m_d_ref,
  covariance_matrix,
  linkage = "single",
  exp_ret_score_tilt_eta = NULL,
  exp_ret_score_tilt = NULL,
  verbose = TRUE
)
}
\arguments{
\item{universe_m_d_ref}{A data.frame with at least columns \code{tickers},
\code{is_eligible}, and \code{exp_ret_score}. Only rows with
\code{is_eligible == 1} are considered.}

\item{covariance_matrix}{A covariance matrix of eligible assets, with row
and column names matching \code{tickers}.}

\item{linkage}{Linkage method for hierarchical clustering. Passed to
\code{\link[stats]{hclust}}. Default is \code{"single"}.}

\item{exp_ret_score_tilt_eta}{numeric or NULL. Tilt intensity used for BOTH "inner" and "final".}

\item{exp_ret_score_tilt}{character or NULL. If "inner", tilt is applied at each split;
if "final", a post overlay is applied; if NULL, no tilt.}

\item{verbose}{Logical, whether to print progress messages. Default is TRUE.}
}
\value{
A list with:
\item{universe_m_d_ref}{Input data.frame merged with portfolio weights.}
\item{weights}{Final portfolio weights (numeric vector).}
\item{dist_matrix}{Distance matrix used in clustering.}
\item{clusters}{\code{hclust} object with the dendrogram.}
}
\description{
Constructs a portfolio using the Hierarchical Risk Parity algorithm
(López de Prado, 2016). Assets are recursively grouped by hierarchical
clustering, and risk is allocated inversely to cluster variances.
Optionally, final weights can be tilted by expected return scores.
}
\references{
López de Prado, M. (2016). Building Diversified Portfolios that
Perform Well Out-of-Sample. The Journal of Portfolio Management, 42(4), 59–69.
}
