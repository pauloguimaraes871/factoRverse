% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{create_liquidity_floor_cutoffs}
\alias{create_liquidity_floor_cutoffs}
\title{Create Liquidity Floor Cutoffs}
\usage{
create_liquidity_floor_cutoffs(metric_name, metric_cutoffs)
}
\arguments{
\item{metric_name}{A character vector of metric names.}

\item{metric_cutoffs}{A list of named numeric vectors, one for each metric in
metric_name. Each vector must have names exactly equal to
c("micro_caps", "small_caps", "mid_caps", "large_caps", "mega_caps") (order may vary).}
}
\value{
A data.frame with a column \code{liquidity_classification} and one column per metric.
The rows are ordered (after reordering each metric vector according to the allowed levels)
so that the main liquidity metric (assumed to be the first element of metric_name)
is in non-decreasing order.
}
\description{
Construct a liquidity_floor_cutoffs data frame from scratch.
}
\details{
The function enforces that:
\itemize{
\item The returned object is a data.frame.
\item The column names (besides \code{liquidity_classification}) are numeric with no NAs.
\item The \code{liquidity_classification} values are exactly the allowed levels:
"micro_caps", "small_caps", "mid_caps", "large_caps", "mega_caps".
\item The main liquidity metric (the first metric in metric_name) is in ascending order.
\item The ordering (ranking) of liquidity classifications is consistent across metrics.
}
}
