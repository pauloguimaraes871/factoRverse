% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-consolidate_meta_objects.R
\name{consolidate_oos_sb_outputs_m_df}
\alias{consolidate_oos_sb_outputs_m_df}
\title{Consolidate Out-of-Sample Signal-Blending Outputs into a Meta Dataframe}
\usage{
consolidate_oos_sb_outputs_m_df(
  base_sb_backtest_outputs_list,
  winsorize_predictions = TRUE,
  normalize_predictions = TRUE,
  winsorization_probs = c(0.025, 0.975),
  features_passthrough_and_positions = "none",
  features_m_df = NULL
)
}
\arguments{
\item{base_sb_backtest_outputs_list}{A list of \code{sb_backtest_results} objects, each containing OOS predictions and associated metadata.}

\item{winsorize_predictions}{Logical; if \code{TRUE}, performs winsorization on predictions to mitigate the effect of outliers. Default is \code{TRUE}.}

\item{normalize_predictions}{Logical; if \code{TRUE}, normalizes predictions to ensure comparability across models. Default is \code{TRUE}.}

\item{winsorization_probs}{A numeric vector of length 2 specifying the lower and upper quantile probabilities for winsorization. Default is \code{c(0.025, 0.975)}.}

\item{features_m_df}{A meta dataframe of features to include in the passthrough. Only required if \code{features_passthrough} is not \code{"none"}. Default is \code{NULL}.}

\item{features_passthrough}{Character; specifies which features from \code{features_m_df} to include in the output. Options are \code{"none"}, \code{"all"}, or specific feature names. Default is \code{"none"}.}
}
\value{
A \code{meta_dataframe} object containing the consolidated predictions, optionally with normalized and winsorized values, and passthrough features.
}
\description{
This function consolidates out-of-sample (OOS) predictions from a list of \code{sb_backtest_results} objects into a single meta dataframe.
It supports optional winsorization and normalization of predictions, as well as passthrough of specific features to the output dataframe.
}
\details{
The function performs a series of validation checks to ensure the consistency of input data:
\itemize{
\item All elements in \code{base_sb_backtest_outputs_list} must have the same \code{oos_predictions_m_df} structure, with matching \code{id} values across all elements.
\item The list must have unique, non-empty names to be used as column identifiers in the consolidated dataframe.
\item If \code{features_passthrough} is not \code{"none"}, a \code{features_m_df} must be provided.
}

After performing these validations, the function consolidates predictions into a single meta dataframe.
Optional winsorization and normalization can be applied to the predictions.
Additionally, the specified features can be passed through to the output dataframe.
}
\examples{
\dontrun{
# Example with default options
consolidated_df <- consolidate_oos_sb_outputs_m_df(
  base_sb_backtest_outputs_list = list_of_sb_results,
  features_passthrough = "all",
  features_m_df = features_df
)
}

}
\seealso{
\linkS4class{sb_backtest_results} for the definition of \code{sb_backtest_results} objects.
\code{\link[=create_meta_dataframe]{create_meta_dataframe()}} for constructing a meta dataframe.
}
