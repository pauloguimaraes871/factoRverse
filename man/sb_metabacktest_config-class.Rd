% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_definitions.R
\docType{class}
\name{sb_metabacktest_config-class}
\alias{sb_metabacktest_config-class}
\title{sb_metabacktest_config Class}
\description{
The sb_metabacktest_config class configures a meta-learning (stacking) backtest:
it wraps a single meta-learner \code{sb_backtest_config} together with the rules for building the
meta feature set from base learners' out-of-sample predictions (feature passthrough,
normalization, winsorization).
}
\section{Slots}{

\describe{
\item{\code{meta_sb_backtest_config}}{A \code{sb_backtest_config} with the configuration for the meta learner}

\item{\code{features_passthrough}}{A character vector indicating which features from \code{features_m_df} are to be passed through to the meta learner.
Alternatively, if \code{'all'}, all features are passed through. If \code{'none'}, no features are passed through. Default is \code{'none'}.}

\item{\code{normalize_base_predictions}}{Logical; if \code{TRUE}, normalizes the base learners' predictions before passing them to the meta learner. Default is \code{TRUE}.}

\item{\code{winsorize_base_predictions}}{Logical; if \code{TRUE}, winsorizes the base learners' predictions before passing them to the meta learner. Default is \code{FALSE}.}

\item{\code{config_name}}{A character string with the name of the configuration}
}}

\section{Validity}{

\itemize{
\item The meta learner's \code{sb_algorithm} may not be \code{rp}/\code{hrp}/\code{mvo}/\code{mmaf}.
\item The meta learner requires a \code{tuning_strategy} unless it is \code{ols} or heuristic.
\item \code{chosen_signals_and_positions} must be \code{"all"} (positions are corrected via \code{features_passthrough}).
\item \code{features_passthrough} may not contain \code{"long"}, \code{"short"} or \code{"force"}.
}
}

