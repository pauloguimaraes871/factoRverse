% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-fit_bayesian_hierarchical_model.R
\name{fit_bayesian_hierarchical_model}
\alias{fit_bayesian_hierarchical_model}
\title{Fit a Bayesian Hierarchical Regression Model and Summarize Posterior Metrics}
\usage{
fit_bayesian_hierarchical_model(
  signal_universe_m_d_ref,
  selected_backtest_returns_corrected_positions_m_xts_upd_ref,
  selected_market_factor_proxy_m_xts_upd_ref,
  selected_backtest_returns_corrected_positions_m_upd_ref = NULL,
  elected_priors,
  selected_signal_themes_m_d_ref,
  model_spec_theme_level,
  chains,
  iter,
  warmup,
  thin,
  seed,
  adapt_delta,
  verbose,
  parallel
)
}
\arguments{
\item{signal_universe_m_d_ref}{A data frame containing the signal universe. If provided, data in this object will be updated with posteriors.}

\item{selected_backtest_returns_corrected_positions_m_xts_upd_ref}{A xts containing the backtest returns data for various signals.}

\item{selected_market_factor_proxy_m_xts_upd_ref}{A numeric vector containing benchmark returns data. The vector will be recycled
to match the length of the backtest returns data.}

\item{selected_backtest_returns_corrected_positions_m_upd_ref}{An already processed \code{selected_backtest_returns_corrected_positions_m_xts_upd_ref}.
This data.frame is already in long format and contemplates both the \code{selected_market_factor_proxy_m_xts_upd_ref} and the
\code{selected_signal_themes_m_d_ref} theme data.}

\item{elected_priors}{An object of class \code{brmsprior} containing priors for the Bayesian model, such as those generated by the
\code{derive_informative_priors_from_data} function or provided directly by the user.}

\item{selected_signal_themes_m_d_ref}{A data frame containing metadata about signals. This data frame should include:
\itemize{
\item \code{tickers}: Signal identifiers matching those in \code{selected_backtest_returns_corrected_positions_m_xts_upd_ref}.
\item \code{theme}: Group membership for each signal, defining the clusters for the Bayesian hierarchical model.
\item \code{dates}: Dates corresponding to the backtest data.
This input ensures proper alignment between signals and their associated themes.
}}

\item{model_spec_theme_level}{A character string specifying the desired Bayesian model structure.
Options include:
\itemize{
\item \code{"random_intercept_fixed_slope"}: Includes random effects for the intercept at the theme level.
\item \code{"theme_specific_intercept_fixed_slope"}: Uses fixed intercepts for each theme.
\item \code{"theme_specific_intercept_theme_specific_slope"}: Includes fixed intercepts and slopes for each theme.
\item \code{"fixed_intercept_fixed_slope"}: Omits theme-level intercepts but includes random effects at the theme:signal level.
}}

\item{chains}{An integer specifying the number of Markov chains to run. Defaults to \code{4}.}

\item{iter}{An integer specifying the number of iterations per chain. Defaults to \code{2000}.}

\item{warmup}{An integer specifying the number of warmup iterations. Defaults to \code{1000}.}

\item{thin}{An integer specifying the thinning interval. Defaults to \code{1}.}

\item{seed}{An integer, or \code{NA}, specifying the random seed for reproducibility. No default; passed through from the caller.}

\item{adapt_delta}{A numeric value between 0 and 1 specifying the target acceptance rate for the Hamiltonian Monte Carlo sampler.}

\item{verbose}{A logical indicating whether to display progress messages during the model fitting process. Defaults to \code{TRUE}.}

\item{parallel}{A logical indicating whether to enable parallel computation using \code{future}. Defaults to \code{TRUE}.}
}
\value{
A named list containing:
\itemize{
\item \code{brm_model}: A \code{brmsfit} object containing the fitted Bayesian model. This object includes posterior distributions,
parameter estimates, diagnostics, and other details of the model fit.
\item \code{signal_universe_m_d_ref}: A data frame containing the updated \code{signal_universe_m_d_ref} with posterior summary statistics,
or \code{NULL} if \code{signal_universe_m_d_ref} was \code{NULL}.
\item \code{posterior_draws_summaries}: A list of median/89\\% CI summaries for the intercept, slope, and scale parameters
(see \code{summarize_posteriors_draws()}), or \code{NULL} if \code{signal_universe_m_d_ref} was \code{NULL}.
}
}
\description{
This function fits a Bayesian hierarchical regression model using the \code{brms} package and computes posterior summary
statistics for signal-level metrics. The model captures signal clustering within themes and provides detailed
posterior metrics for use in performance analysis.
}
\details{
The function performs the following steps:
\enumerate{
\item \strong{Preprocessing}: Converts the backtest returns data into long format, merges metadata about signals and themes,
and adds the market factor proxy.
\item \strong{Formula Specification}: Defines the Bayesian model formula based on the chosen \code{model_spec_theme_level}.
\item \strong{Model Fitting}: Fits the Bayesian model using the \code{brm} function from the \code{brms} package, incorporating the
provided priors and prepared data.
\item \strong{Posterior Summarization}: Extracts posterior draws and computes signal-level metrics such as alphas, betas,
sigmas, active returns, tracking errors, information ratios (IR), appraisal ratios (AP), and Treynor ratios.
Updates the \code{signal_universe_m_d_ref} data frame with these metrics.
}

This hierarchical approach allows the model to capture both global (theme-level) and local (signal-level) effects
while leveraging the flexibility of Bayesian regression. The inclusion of posterior metrics enables deeper
performance analysis of signals.
}
