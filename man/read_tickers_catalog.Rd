% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_tickers_catalog.R
\name{read_tickers_catalog}
\alias{read_tickers_catalog}
\alias{read_tickers_catalog,raw_features_m_df,tickers_catalog-method}
\alias{read_tickers_catalog,returns_meta_xts,tickers_catalog-method}
\title{Apply tickers_catalog Transformations}
\usage{
read_tickers_catalog(data, tickers_catalog, ...)

\S4method{read_tickers_catalog}{raw_features_m_df,tickers_catalog}(data, tickers_catalog, verbose = TRUE)

\S4method{read_tickers_catalog}{returns_meta_xts,tickers_catalog}(data, tickers_catalog, verbose = TRUE)
}
\arguments{
\item{data}{An object of class \code{raw_features_m_df} or \code{returns_meta_xts}.}

\item{tickers_catalog}{A \code{tickers_catalog} object containing reference data including
ticker status, trading range, and permanent IDs.}

\item{...}{Additional arguments passed to methods. Supports \code{verbose = TRUE/FALSE}.}

\item{verbose}{A logical indicating whether to print progress messages. Defaults to \code{TRUE}.}
}
\value{
An object of the same class as \code{data}, modified according to the catalog rules.
}
\description{
Applies ticker ID mapping and filtering based on a \code{tickers_catalog} object. Can handle either
a \code{raw_features_m_df} or a \code{returns_meta_xts} object. This ensures consistency in ticker
identifiers and filters out data outside trading ranges or for untraded tickers.
}
\details{
The catalog's stable \code{perm_id} values replace the (possibly changing) \code{tickers} labels, so a company keeps a
single identity across ticker renames. Each series is then restricted to its valid trading window: observations
before \code{tickers_first_quote} or after \code{tickers_last_quote} (plus the catalog's \code{n_days_tolerance}) are set to NA
or dropped, and tickers classified as \code{untraded} are removed. This yields a clean, survivorship-aware panel
ready for the silver/gold preprocessing stages.
}
\seealso{
\code{\link{create_tickers_catalog}}, \code{\link{update_tickers_catalog}}, \code{\link{tickers_catalog-class}}
}
