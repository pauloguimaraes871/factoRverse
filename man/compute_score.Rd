% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_score.R
\name{compute_score}
\alias{compute_score}
\alias{compute_score,meta_dataframe,list,character-method}
\title{Compute Score Based on Conditions}
\usage{
compute_score(features_m_df, conditions, feature_name, ...)

\S4method{compute_score}{meta_dataframe,list,character}(
  features_m_df,
  conditions,
  feature_name = "score",
  min_non_na = 0
)
}
\arguments{
\item{features_m_df}{A \code{meta_dataframe} object.}

\item{conditions}{A named list of functions. Each name must correspond to a column
in the data, and each function should return a logical vector indicating whether
the condition is met.}

\item{feature_name}{A \code{character} string specifying the name of the new score column.}

\item{...}{Additional arguments passed to the conditions.}

\item{min_non_na}{A numeric value indicating the minimum number of non-NA values required
to compute the score for a given row. Defaults to \code{0}.}
}
\value{
A \code{meta_dataframe} object with an additional column (named \code{feature_name})
containing the number of conditions met for each row (or \code{NA} if insufficient data).
}
\description{
Computes a custom score by evaluating multiple user-defined conditions on columns
of a \code{meta_dataframe}. Each condition corresponds to a variable and is applied
row-wise. For each row, the number of satisfied conditions is counted.

If the number of non-NA inputs for that row is below \code{min_non_na}, the score is set to \code{NA}.
}
\details{
This function is useful for constructing composite scores based on multiple thresholds
or logical criteria. Each condition is applied to its corresponding column using \code{purrr::map2()}.
The total score per row reflects how many of the defined conditions are satisfied.
}
\examples{
\dontrun{
# Count how many quality criteria each stock meets
conditions <- list(roe_3m = function(x) x > 0.1, net_margin = function(x) x > 0.05)
features_m_df <- compute_score(features_m_df, conditions, feature_name = "quality_score")
}

}
