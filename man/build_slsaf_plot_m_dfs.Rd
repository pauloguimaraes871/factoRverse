% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-derive_slsaf_leg_diagnostics.R
\name{build_slsaf_plot_m_dfs}
\alias{build_slsaf_plot_m_dfs}
\title{Build Plot-Ready Objects for a SLSAF Portfolio Backtest}
\usage{
build_slsaf_plot_m_dfs(
  stock_universe_m_df,
  selected_benchmark,
  group_col = NULL
)
}
\arguments{
\item{stock_universe_m_df}{The backtest universe, as produced by an \code{slsaf} run.}

\item{selected_benchmark}{Character scalar naming the benchmark.}

\item{group_col}{Optional character naming the sector column.}
}
\value{
A named list of \code{meta_dataframe} objects (\code{budget}, \code{coverage}, \code{leg_score},
\code{leg_risk}, \code{underweight}) plus \code{universe_with_leg}, the input universe augmented
with a \code{leg} label, and \code{diagnostics}, the raw aggregates.
}
\description{
Wraps the aggregates from \code{\link{derive_slsaf_leg_diagnostics}} into
\code{meta_dataframe} objects so the existing plot methods can render them directly. Every
\code{slsaf} plot is therefore an ordinary \code{meta_dataframe} plot over a purpose-built
frame, rather than bespoke plotting code.
}
\details{
The \code{meta_dataframe} contract requires an \code{id}, \code{tickers} and \code{dates} triple, with
\code{id} equal to \code{tickers-dates}. The aggregates are not per-asset, so the \code{tickers}
slot is used to carry whatever the series is keyed by: the leg name, the budget
component, or the asset itself for the per-name profile. This is the same device the
macro-level plots use to render group series.
}
