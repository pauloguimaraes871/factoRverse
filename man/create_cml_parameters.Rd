% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{create_cml_parameters}
\alias{create_cml_parameters}
\title{Create cml_parameters}
\usage{
create_cml_parameters(
  residual_ticker,
  target,
  target_metric = c("tracking_error", "volatility"),
  p = 1,
  vol_source = c("ex_ante", "realized_rolling", "supplied"),
  vol_cov_est_method = NULL,
  vol_window = 6,
  min_weight = 0,
  max_weight = 1
)
}
\arguments{
\item{residual_ticker}{Character naming the residual sleeve. It must be a row of the data
objects the backtest runs on, carrying its own return, liquidity and volatility.}

\item{target}{Numeric, in the target metric's own units.}

\item{target_metric}{\code{"tracking_error"} (default) or \code{"volatility"}. Must match what the
residual is: an index-tracking residual for the former, a riskless one for the latter.}

\item{p}{Numeric exponent, 1 for risk targeting and 2 for the inverse-variance response.}

\item{vol_source}{\code{"ex_ante"} (default), \code{"realized_rolling"} or \code{"supplied"}.}

\item{vol_cov_est_method}{A \code{cov_est_method} for \code{"ex_ante"}. Defaults to EWMA over 60 daily
observations with \code{active_returns = FALSE}, since a tracking-error target is expressed
through active weights rather than by subtracting the benchmark from the returns.}

\item{vol_window}{Numeric months for \code{"realized_rolling"}. Default 6.}

\item{min_weight, max_weight}{Numeric bounds on the risky sleeve. Default 0 and 1.}
}
\value{
An object of class \code{cml_parameters}.
}
\description{
Builds the parameters for the \code{risk_targeted} meta-portfolio path, which scales a risky sleeve
against a residual sleeve so the combination targets a stated level of risk. See
\linkS4class{cml_parameters} for how the residual and the target metric have to match, and for what
each \code{vol_source} measures.
}
\examples{
\dontrun{
  # Hold at least half the risky sleeve, targeting 4 percent annualised tracking error
  cml_params <- create_cml_parameters(
    residual_ticker = "BOVA11", target = 4, target_metric = "tracking_error",
    p = 1, min_weight = 0.5
  )
}
}
\seealso{
\linkS4class{cml_parameters}, \code{\link[=add_cml_parameters]{add_cml_parameters()}}
}
