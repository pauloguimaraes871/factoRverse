% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_methods.R
\name{plot,meta_xts,missing-method}
\alias{plot,meta_xts,missing-method}
\title{Plot Method for meta_xts}
\usage{
\S4method{plot}{meta_xts,missing}(
  x,
  y,
  clustering_list = NULL,
  facet_by_year = NULL,
  add_overall_means = NULL,
  vertical_lines = NULL,
  cumulative = NULL,
  plot_perf_metric = NULL,
  benchmark_returns_m_xts = NULL,
  active_returns = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{meta_xts} (or subclass) object.}

\item{y}{Not used (for consistency with base \code{plot} generic).}

\item{clustering_list}{A named list, where each name (e.g. "Base") is mapped to
a character vector of column names. If \code{NULL}, no grouping is performed.}

\item{...}{Further arguments passed to or from other methods (currently not used).}
}
\value{
Invisibly returns the ggplot object. It also prints a legend mapping
numeric labels to the original column names.
}
\description{
Plots data from a meta_xts (or subclass) object using ggplot2, labeling each
distinct series by its column name. Optionally allows grouping of series by
a \code{clustering_list}.
}
\details{
\enumerate{
\item Converts the \code{xts} data to a data frame, then melts it into long format,
\item Assigns each series (column) a numeric color label,
\item Optionally groups series by a provided \code{clustering_list} to vary line type,
\item Specifies \code{group = series} (or \code{group = interaction(series, group)}) so ggplot2
knows which observations to connect by line,
\item Plots the data on a dark background with neon color palette.
}
}
\examples{
\dontrun{
library(xts)

# Create example data
test_dates <- seq(as.Date("2023-01-15"), by="month", length.out=7)
test_data <- xts(matrix(c(0.086, -0.014, -0.026, -0.040, -0.073, 0.115, 0.101,
                          0.080,  0.053,  0.087,  0.079,  0.060, 0.095, 0.070,
                          NA,      NA,      NA,      NA,      NA,   NA, -0.063),
                        nrow=7, ncol=3, byrow=FALSE,
                        dimnames=list(NULL, c("series_1", "series_2", "series_3"))),
                 order.by = test_dates)

# Build a dummy meta_xts
my_meta <- new("metrics_meta_xts",
               data = test_data,
               meta_xts_name = "Example",
               metric_name = "rss",
               workflow = list(),
               n_dates = nrow(test_data),
               source = rep("MockSource", 3),
               frequency = "monthly",
               series = colnames(test_data),
               n_metrics = ncol(test_data))

# Optional clustering
my_clusters <- list("Group1" = "series_1", "Group2" = "series_2")

# Plot (with or without clustering_list)
plot(my_meta)
plot(my_meta, clustering_list = my_clusters)
}

}
