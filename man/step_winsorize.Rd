% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_recipe_timewise.R
\name{step_winsorize}
\alias{step_winsorize}
\title{Step for Winsorization}
\usage{
step_winsorize(
  recipe,
  ...,
  role = NA,
  trained = FALSE,
  probs = c(0.05, 0.95),
  skip = FALSE,
  id = recipes::rand_id("winsorize")
)
}
\arguments{
\item{recipe}{A \code{recipe} object. The step will be added to the sequence of operations for this recipe.}

\item{...}{One or more selector functions to choose which variables to apply winsorization to. If empty, all numeric variables are selected.}

\item{role}{Not used by this step since no new variables are created. Defaults to \code{NA}.}

\item{trained}{A logical indicating whether the step has been trained (i.e., thresholds have been calculated).}

\item{probs}{A numeric vector of length 2 indicating the lower and upper quantile probabilities used to determine winsorization limits. Default is \code{c(0.05, 0.95)}.}

\item{skip}{A logical. Should the step be skipped when the recipe is baked by \code{recipes::bake()}? Default is \code{FALSE}.}

\item{id}{A character string used to identify this step. Defaults to a random string generated by \code{recipes::rand_id("winsorize")}.}
}
\value{
An updated \code{recipe} object with the \code{step_winsorize} added to the sequence of steps.
}
\description{
\code{step_winsorize()} creates a \emph{recipe step} that applies winsorization to numeric variables.
Winsorization replaces extreme values below or above specified quantile thresholds with those threshold values.

This step is useful to reduce the influence of outliers without fully trimming observations.
}
\details{
The actual winsorization limits are computed during the \code{prep()} phase using the quantiles of the training data.
During the \code{bake()} phase, all values below the lower quantile threshold are replaced with that threshold,
and values above the upper quantile threshold are similarly adjusted.
}
