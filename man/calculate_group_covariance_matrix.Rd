% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-compute_agg_macro_objects.R
\name{calculate_group_covariance_matrix}
\alias{calculate_group_covariance_matrix}
\title{Compute a group-by-group (block) covariance matrix}
\usage{
calculate_group_covariance_matrix(
  eligible_universe_m_d_ref,
  groups,
  covariance_matrix,
  group_col,
  micro_universe_m_d_ref_list = NULL
)
}
\arguments{
\item{eligible_universe_m_d_ref}{A data.frame containing at least the
columns \code{tickers} (character) and the grouping column indicated by
\code{group_col}. Only rows with non-\code{NA} \code{group_col} values are used.}

\item{groups}{Character vector of group names (e.g., sectors) for which to
compute the covariance matrix. Must match values in
\code{eligible_universe_m_d_ref[[group_col]]}.}

\item{covariance_matrix}{Numeric covariance matrix \eqn{\Sigma} with row and
column names equal to asset tickers. Must be square and cover all tickers
present in \code{eligible_universe_m_d_ref$tickers}.}

\item{group_col}{Character scalar with the name of the column in
\code{eligible_universe_m_d_ref} that defines group membership (e.g., \code{"sector"}).}

\item{micro_universe_m_d_ref_list}{Named list whose names are the elements of
\code{groups}. Each element is a data.frame with columns:
\itemize{
\item \code{tickers} (character): tickers belonging to that group; and
\item \code{weights} (numeric): corresponding weights for the group-level
aggregation.
}}
}
\value{
A numeric \eqn{G \times G} matrix with dimnames set to \code{groups},
where entry \eqn{(g_1, g_2)} equals
\eqn{w_{g_1}^\top \, \Sigma_{g_1,g_2} \, w_{g_2}}.
}
\description{
Builds a \eqn{G \times G} matrix of covariances between groups (e.g., sectors)
using a full asset-level covariance matrix \eqn{\Sigma} and per-group weight
vectors. Each entry \eqn{(g_1, g_2)} is computed as
\deqn{w_{g_1}^\top \, \Sigma_{g_1,g_2} \, w_{g_2},}
where \eqn{\Sigma_{g_1,g_2}} is the block of \eqn{\Sigma} whose rows are the
tickers in group \eqn{g_1} and columns are the tickers in group \eqn{g_2}.
}
\details{
\strong{Input requirements and alignment}
\itemize{
\item All tickers in \code{eligible_universe_m_d_ref$tickers} must be present as both
row and column names of \code{covariance_matrix}.
\item For each \code{g} in \code{groups}, \code{micro_universe_m_d_ref_list[[g]]} must include
\code{tickers} and \code{weights} for \strong{all} tickers that belong to group \code{g}
according to \code{eligible_universe_m_d_ref[[group_col]]}. Internally, weights
are aligned to the ticker order used to extract the \eqn{\Sigma} blocks.
}

\strong{Normalization}
The function does \strong{not} renormalize group weights; results therefore reflect
the absolute scaling implied by the provided weights. If you need pure
within-group aggregation (i.e., each group’s weights summing to 1), normalize
the weights in \code{micro_universe_m_d_ref_list} beforehand.

\strong{Complexity}
The computation is \eqn{O(G^2)} block multiplications, where \eqn{G = } \code{length(groups)},
plus the cost of extracting each block from \eqn{\Sigma}.
}
\note{
This function assumes \code{eligible_universe_m_d_ref$tickers} are unique within
each group for the given snapshot. If duplicates may occur, deduplicate or
aggregate beforehand.
}
\section{Errors and Validation}{

The function raises errors when:
\itemize{
\item \code{covariance_matrix} is not a numeric matrix with named rows/columns.
\item \code{groups} is empty or not a character vector.
\item \code{micro_universe_m_d_ref_list} is not a named list covering all \code{groups}.
\item Any ticker in \code{eligible_universe_m_d_ref$tickers} is missing from
\code{covariance_matrix} row/column names.
\item For any group, some required tickers are missing in the corresponding
\code{micro_universe_m_d_ref_list[[g]]$tickers}, or the weights frame lacks the
required \code{tickers}/\code{weights} columns.
}
}

\seealso{
\itemize{
\item \code{stats::cov} for covariance computation at the asset level (if needed upstream).
\item \code{dplyr::filter}, \code{dplyr::pull}, \code{dplyr::slice} and \code{rlang::sym} used here for data handling.
}
}
