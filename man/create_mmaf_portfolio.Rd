% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-create_mmaf_portfolio.R
\name{create_mmaf_portfolio}
\alias{create_mmaf_portfolio}
\title{Create a Micro–Macro Allocation Framework (MMAF) portfolio}
\usage{
create_mmaf_portfolio(
  universe_m_d_ref,
  mmaf_method = "bottom_up",
  eligible_returns_m_xts_upd_ref = NULL,
  covariance_matrix = NULL,
  selected_benchmark_m_xts_upd_ref = NULL,
  active_returns = if (is.null(selected_benchmark_m_xts_upd_ref)) FALSE else TRUE,
  cov_estimation_method = "sample",
  cov_matrix_sample_size = if (is.null(eligible_returns_m_xts_upd_ref)) NULL else
    nrow(eligible_returns_m_xts_upd_ref),
  groups_m_d_ref,
  mmaf_group_col,
  liquidity_m_d_ref,
  top_down_proxy_port_method = "rp",
  micro_port_construction_method,
  linkage = "single",
  liquidity_constraint_policy = NULL,
  turnover_constraint_policy = NULL,
  concentration_constraint_policy = NULL,
  cap_weighting_metric = NULL,
  n_random_ports = 2000,
  random_ports_method = "sample",
  opt_objective = "sharpe",
  opt_method = "random",
  ridge_pen = NULL,
  n_resamples = 0,
  exp_ret_score_jitter = 0,
  cov_eigval_jitter = 0,
  rp_method = "cyclical-spinu",
  exp_ret_score_tilt = NULL,
  exp_ret_score_tilt_eta = NULL,
  macro_port_construction_method,
  macro_concentration_constraint_policy = NULL,
  macro_cap_weighting_metric = NULL,
  macro_n_random_ports = 2000,
  macro_random_ports_method = "sample",
  macro_opt_objective = "sharpe",
  macro_opt_method = "random",
  macro_ridge_pen = NULL,
  macro_n_resamples = 0,
  macro_exp_ret_score_jitter = 0,
  macro_cov_eigval_jitter = 0,
  macro_rp_method = "cyclical-spinu",
  macro_exp_ret_score_tilt = NULL,
  macro_exp_ret_score_tilt_eta = NULL,
  macro_linkage = "single",
  selected_benchmark = NULL,
  bench_assets_returns_m_xts_upd_ref = NULL,
  lower_quantile_winsorization = 0.025,
  upper_quantile_winsorization = 0.975,
  parallel = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{universe_m_d_ref}{A data.frame/tibble with (at least) columns:
\code{id}, \code{tickers}, \code{dates}, \code{is_eligible}, \code{exp_ret_score}, and any optional
columns used by the micro methods (e.g., benchmark/target weights, liquidity features).
Should refer to a \strong{single date}.}

\item{mmaf_method}{Character scalar: \code{"top_down"} or \code{"bottom_up"}.}

\item{eligible_returns_m_xts_upd_ref}{An optional \code{xts} object containing return data for the eligible tickers, used in covariance matrix estimation for Risk-Parity and MVO methods.}

\item{covariance_matrix}{Numeric covariance matrix with row/col names that
exactly match the \code{tickers} of eligible names (same ordering is enforced).}

\item{selected_benchmark_m_xts_upd_ref}{An optional \code{xts} object containing benchmark returns used to compute active returns (only if \code{active_returns = TRUE}).}

\item{active_returns}{Logical. If \code{TRUE}, covariance estimation will use active returns (asset returns minus benchmark). Defaults to \code{FALSE} if \code{selected_benchmark_m_xts_upd_ref} is \code{NULL}, otherwise \code{TRUE}.}

\item{cov_estimation_method}{An optional character string specifying the method for estimating the covariance matrix. Defaults to \code{NULL}.}

\item{cov_matrix_sample_size}{Integer. Number of time periods (rows in \code{eligible_returns_m_xts_upd_ref}) used to estimate the covariance matrix. If \code{NULL}, uses all available observations.}

\item{groups_m_d_ref}{An optional data frame used for group constraints and covariance matrix estimation. Should include group information if used. Defaults to \code{NULL}.}

\item{mmaf_group_col}{Character scalar naming the column in \code{groups_m_d_ref} that carries
the group/sector classification. (Your wrapper enforces this as the 4th column.)}

\item{liquidity_m_d_ref}{Optional data.frame/tibble with liquidity features aligned by
\code{id}, \code{tickers}, \code{dates}. If provided, group-level liquidity metrics are aggregated
as weighted means of intra-sector micro weights.}

\item{top_down_proxy_port_method}{Character: micro method to build \emph{proxy} intra-sector
portfolios in the initial pass of \code{"top_down"} (e.g., \code{"rp"}, \code{"hrp"}, \code{"ew"}).}

\item{micro_port_construction_method}{Character micro method used for the actual
intra-sector optimization (e.g., \code{"mvo"}, \code{"rp"}, \code{"hrp"}, \code{"ew"}, or custom handled
by \code{set_portfolio_weights()}).}

\item{linkage}{Character, passed to hierarchical methods when applicable.}

\item{liquidity_constraint_policy}{Optional list describing liquidity caps at micro level.}

\item{turnover_constraint_policy}{Optional list describing turnover caps at micro level.}

\item{concentration_constraint_policy}{Optional list with e.g.
\code{max_abs_active_individual_weight} for micro level.}

\item{cap_weighting_metric}{Optional character for capitalization weighting metric (if used).}

\item{n_random_ports}{Integer, number of random portfolios (for MVO/random search).}

\item{random_ports_method}{Character, sampling method for random portfolios.}

\item{opt_objective}{Character, optimization objective (e.g., \code{"sharpe"}).}

\item{opt_method}{Character, optimizer selector for MVO/random search.}

\item{ridge_pen}{Numeric or \code{NULL}, ridge penalty for MVO (micro).}

\item{n_resamples}{Integer, number of resamples for robust MVO (micro).}

\item{exp_ret_score_jitter}{Numeric, jitter on expected return score (micro MVO).}

\item{cov_eigval_jitter}{Numeric, jitter on covariance eigenvalues (micro MVO).}

\item{rp_method}{Character, risk parity method at micro level (e.g., \code{"cyclical-spinu"}).}

\item{exp_ret_score_tilt}{Optional numeric vector/column name for RP tilt (micro).}

\item{exp_ret_score_tilt_eta}{Optional numeric, tilt intensity for micro RP.}

\item{macro_port_construction_method}{Character macro method used to allocate across groups
(e.g., \code{"ew"}, \code{"rp"}, \code{"hrp"}, \code{"mvo"}). For a strictly \emph{neutral} top-down
sector allocation, prefer \code{"ew"}, \code{"rp"} or \code{"hrp"} and keep \code{macro_exp_ret_score_tilt = NULL}.}

\item{macro_concentration_constraint_policy}{Optional list with group-level weight caps.}

\item{macro_cap_weighting_metric}{Optional character for cap-based macro weighting (if used).}

\item{macro_n_random_ports}{Integer, number of random portfolios at macro level.}

\item{macro_random_ports_method}{Character, sampling method (macro).}

\item{macro_opt_objective}{Character, optimization objective at macro.}

\item{macro_opt_method}{Character, optimizer selector at macro.}

\item{macro_ridge_pen}{Numeric or \code{NULL}, ridge penalty for macro MVO.}

\item{macro_n_resamples}{Integer, number of resamples for macro MVO.}

\item{macro_exp_ret_score_jitter}{Numeric, jitter on sector ER (macro MVO).}

\item{macro_cov_eigval_jitter}{Numeric, jitter on macro covariance eigenvalues.}

\item{macro_rp_method}{Character, risk parity method at macro.}

\item{macro_exp_ret_score_tilt}{Optional numeric vector/column name for RP tilt (macro).}

\item{macro_exp_ret_score_tilt_eta}{Optional numeric, tilt intensity for macro RP.}

\item{macro_linkage}{Character, passed to macro hierarchical methods when applicable.}

\item{selected_benchmark}{It controls whether a 'port' object for the benchmark should be created and added to portolio results.}

\item{bench_assets_returns_m_xts_upd_ref}{Optional. A 'xts' object containing returns for all stocks. Needed for computing benchmark and port stats.}

\item{lower_quantile_winsorization, upper_quantile_winsorization}{Numerics in (0,1),
passed through to micro and macro \code{set_portfolio_weights()}.}

\item{parallel}{Logical, whether to parallelize micro-level sector runs via \code{furrr::future_map()}.}

\item{verbose}{Logical, print progress and timing via \code{tictoc}.}
}
\value{
A list with:
\describe{
\item{universe_m_d_ref}{Input universe joined with final \code{weights} (stock-level).}
\item{group_weights}{Named numeric vector of sector weights (macro).}
\item{macro}{Return object from \code{set_portfolio_weights()} at macro level.}
\item{micro}{Top-down: list of per-sector micro return objects. Bottom-up:
a single-element list \verb{list(consolidated = <micro_object>)}.}
\item{group_cov_matrix}{Sector-by-sector covariance matrix implied by micro solutions.}
}
}
\description{
Implements a two-layer portfolio construction framework where the behavior is
controlled by \code{mmaf_method}:
\itemize{
\item \strong{"top_down"}: Build \emph{proxy} intra-sector portfolios first (no sector budgets),
aggregate to sector-level metrics, construct sector (macro) weights, then
re-optimize \strong{inside each sector} under its sector budget (constraints scaled
by the sector budget), and finally reconcile to final stock weights
(sector weight × intra-sector weight).
\item \strong{"bottom_up"}: Build a single micro-level portfolio across all stocks,
aggregate the resulting weights to sector-level metrics, construct sector
weights (macro), then reconcile stock weights so sector totals match macro weights.
(Micro-level concentration constraints may not hold globally after reconciliation.)
}
}
\details{
\strong{Top-down} emphasizes sector allocation: sectors are first represented by proxy
intra-sector portfolios to estimate sector ER/liquidity and the sector-by-sector
covariance. After optimizing sector weights (macro), intra-sector portfolios are
re-optimized under sector budgets. Constraint policies (concentration/turnover/liquidity)
are \strong{scaled by the sector budget} when applicable.

\strong{Bottom-up} emphasizes stock selection: a single micro portfolio is built across
all names. Sector metrics are aggregated from this solution, macro sector weights
are computed, and final stock weights are reconciled to match macro sector totals.
Micro-level concentration constraints may no longer hold exactly after reconciliation.

The function assumes (by your upstream contract) that:
\itemize{
\item \code{universe_m_d_ref} / \code{groups_m_d_ref} / \code{liquidity_m_d_ref} are all single-date.
\item \code{covariance_matrix} row/col names match the eligible tickers (same ordering).
\item \code{port@universe_m_d_ref@data} carries liquidity columns when those are used.
\item \code{mmaf_group_col} is the 4th column of \code{groups_m_d_ref} (enforced by a wrapper).
}
}
\examples{
\dontrun{
# Minimal sketch (objects must be prepared consistently upstream):
res_td <- create_mmaf_portfolio(
  universe_m_d_ref = universe_df,
  mmaf_method = "top_down",
  covariance_matrix = Sigma,
  groups_m_d_ref = groups_df,
  mmaf_group_col = "sector",
  liquidity_m_d_ref = liq_df,
  top_down_proxy_port_method = "rp",
  micro_port_construction_method = "mvo",
  macro_port_construction_method = "hrp",
  verbose = TRUE
)

res_bu <- create_mmaf_portfolio(
  universe_m_d_ref = universe_df,
  mmaf_method = "bottom_up",
  covariance_matrix = Sigma,
  groups_m_d_ref = groups_df,
  mmaf_group_col = "sector",
  liquidity_m_d_ref = liq_df,
  micro_port_construction_method = "mvo",
  macro_port_construction_method = "rp",
  verbose = TRUE
)

# Check reconciliation by sector (example):
# dplyr::summarise sum of final weights by sector should match res_td$group_weights
}

}
