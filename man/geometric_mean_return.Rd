% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_window.R
\name{geometric_mean_return}
\alias{geometric_mean_return}
\title{Geometric Mean Return (percentage-point scale)}
\usage{
geometric_mean_return(
  returns,
  na.rm = FALSE,
  mult_last_n = 0,
  mult_by = 0,
  scale = 100
)
}
\arguments{
\item{returns}{Numeric vector of returns. Each return must be greater than
\code{-100} (i.e. no return worse than -100 \%).}

\item{na.rm}{Logical. Should \code{NA} values be removed?  Default \code{FALSE}.}

\item{mult_last_n}{= Integer >= 0. If > 0, multiply the last n (in time order) by a number.}

\item{mult_by}{Numeric scalar. The number to multiply the last n values by. Default is 0 (skip).}

\item{scale}{Numeric divisor/multiplier used to convert between percentage
points and decimals.  Default \code{100}.}
}
\value{
A single numeric value (in percentage points) representing the
compounded average return.
}
\description{
Computes the geometric mean of a vector of returns expressed \strong{in percentage
points} (e.g. \code{2} for 2 \%, \code{-0.75} for -0.75 \%).
Internally the function converts the input to decimal form, performs the
compounding, and returns the result \strong{in the same percentage-point scale}.
}
\details{
The computation is
\deqn{\left(\prod_{i=1}^{n}\left(1 + \frac{r_i}{\text{scale}}\right)\right)^{1/n} - 1}
converted back to percentage-point scale by multiplying the decimal result by
\code{scale}.
}
\examples{
# 2 \% , 0.5 \% and -1 \% expressed as 2, 0.5, -1
geometric_mean_return(c(2, 0.5, -1))        # ≈ 0.498 (\% points)
geometric_mean_return(c(2, NA, 1), na.rm = TRUE)

}
