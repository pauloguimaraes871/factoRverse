% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_methods.R
\name{predict,keras_ensemble-method}
\alias{predict,keras_ensemble-method}
\title{Predict Method for keras_ensemble Class}
\usage{
\S4method{predict}{keras_ensemble}(object, x, ...)
}
\arguments{
\item{object}{An instance of the \code{keras_ensemble} class, as returned in the
\code{model_nn} element of \code{\link[=fit_keras_model]{fit_keras_model()}} when \code{n_ensembles > 1}.}

\item{x}{A numeric matrix of features, with one row per observation and columns
in the same order the members were trained on.}

\item{...}{Further arguments passed to the members' own \code{predict} methods.}
}
\value{
A numeric vector with one averaged prediction per row of \code{x}.
}
\description{
Generates a forecast from an ensemble of independently initialised Keras
networks by averaging the forecasts of its members.
}
\details{
Forecasts are averaged, not weights: hidden units are permutation symmetric,
so the coordinates of independently initialised networks are not comparable
and averaging weights would be meaningless.

Averaging is done with \code{\link[=Reduce]{Reduce()}} rather than by binding member predictions
into a matrix, because a single-row \code{x} (a one-asset cross-section, which does
occur at sparse rebalancing dates) would collapse such a matrix to a bare
vector and silently average across members instead of across rows.
}
