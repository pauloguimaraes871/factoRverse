% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{create_port_backtest_cohort}
\alias{create_port_backtest_cohort}
\title{Create Portfolio Backtest Cohort}
\usage{
create_port_backtest_cohort(port_backtest_results_list, cohort_name)
}
\arguments{
\item{port_backtest_results_list}{A list of \code{port_backtest_results} objects.}

\item{cohort_name}{A character string representing the name for the merged cohort.}
}
\value{
An object of class \code{port_backtest_cohort}.
}
\description{
This function creates a \code{port_backtest_cohort} object by merging a list of
\code{port_backtest_results} objects. It checks for compatibility across backtests by
verifying that key elements of the \code{port_backtest_workflow} slot are identical.
}
\details{
The function then merges:
\itemize{
\item{\strong{port_weights_m_df:}} Merges the underlying meta data.frames (accessed via \verb{@data}) by matching \code{id}, \code{tickers} and \code{dates}.
The individual \code{eop_port_weights} columns are renamed to the corresponding backtest identifier.
If a benchmark was used (i.e. \code{selected_benchmark} is not \code{NULL}), the common \code{bench_weights} column is
checked for consistency and included only once.
\item{\strong{port_costs_m_xts:}} For each cost type (direct_cost, market_impact_cost, total_cost, turnover),
the function extracts the respective column from each backtest’s \verb{@port_costs_m_xts@data},
renames it to the backtest identifier, and then merges them (after checking that dates match).
\item{\strong{port_returns_m_xts:}} Separately merges columns for raw_return, net_return, raw_active_return, and net_active_return.
For raw_return and net_return, if a benchmark is present the common \code{selected_bench_return} column is included
after checking consistency.
\item{\strong{port_metrics_m_xts:}} As the metrics may be built from custom stock metrics,
the function iterates over the primary metric columns (i.e. those not starting with \code{bench_})
and merges them across backtests. If a corresponding bench column exists (and benchmarks are used)
and is identical across backtests, it is included once.
}

After merging, the function uses \code{create_meta_dataframe} and \code{create_meta_xts} (from your package)
to reconstruct the meta objects. The resulting \code{port_backtest_cohort} object contains the merged data
and the common backtest workflow parameters.
}
