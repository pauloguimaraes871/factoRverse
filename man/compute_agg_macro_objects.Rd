% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-compute_agg_macro_objects.R
\name{compute_agg_macro_objects}
\alias{compute_agg_macro_objects}
\title{Build aggregated group-level universe objects (and optional group covariance)}
\usage{
compute_agg_macro_objects(
  universe_m_d_ref,
  covariance_matrix = NULL,
  group_col,
  micro_universe_m_d_ref_list = NULL,
  liquidity_m_d_ref = NULL
)
}
\arguments{
\item{universe_m_d_ref}{\code{data.frame}. Must contain at least:
\itemize{
\item \code{tickers} (\code{character}): asset identifiers;
\item \code{dates} (\code{Date}): snapshot date;
\item \code{is_eligible} (\code{integer}/\code{logical}): must be 1 or 0;
\item \code{exp_ret_score} (\code{numeric}): stock-level expected return score (used in aggregation);
\item \code{weights} (\code{numeric}): portfolio weights for the snapshot (used to aggregate within group);
\item \code{{group_col}} (\code{character}): grouping column name provided via \code{group_col}.
}}

\item{covariance_matrix}{\code{matrix} or \code{NULL}. Optional \strong{asset-level} covariance
\eqn{\Sigma} whose row/column names are exactly \code{eligible_universe_m_d_ref$tickers}.
If provided, a \strong{group covariance matrix} is computed via
\eqn{\Sigma_G[g_1,g_2] = w_{g_1}^\top \Sigma_{g_1,g_2} w_{g_2}} using the
per-group weights in \code{micro_universe_m_d_ref_list}.}

\item{group_col}{\code{character(1)}. Column name in \code{eligible_universe_m_d_ref} that
defines group membership (e.g., \code{"Sector"}).}

\item{micro_universe_m_d_ref_list}{\code{NULL} or \verb{named list}. Optional override for
within-group weights. Names must cover \strong{all} groups present in
\code{eligible_universe_m_d_ref[[group_col]]}. Each element is a \code{data.frame}
with columns:
\itemize{
\item \code{tickers} (\code{character});
\item \code{weights} (\code{numeric}) — expected to be \strong{within-group} weights (the function
will normalize if the sum is nonzero; if the sum is zero, it falls back to EW with warning).
When \code{NULL}, the function constructs this list from \code{eligible_universe_m_d_ref}.
}}

\item{liquidity_m_d_ref}{\code{NULL} or \code{data.frame}. Optional stock-level liquidity
metrics aligned by \code{id}/\code{tickers}/\code{dates}. All columns \strong{after} the first three
(id, tickers, dates) are aggregated by \strong{weighted mean} using the group's
within-group weights. If \code{NULL}, no liquidity aggregation is produced.}
}
\value{
\code{list} with components:
\itemize{
\item \code{group_universe_m_d_ref} (\code{data.frame}): one row per group containing:
\itemize{
\item \code{id} (\code{character}): \code{"group-current_date"};
\item \code{tickers} (\code{character}): group identifier (the group name);
\item \code{dates} (\code{Date}): snapshot date;
\item \verb{\{liquidity columns\}} (\code{numeric}, optional): weighted means of each
liquidity metric provided in \code{liquidity_m_d_ref} (if any);
\item \verb{\{*_bench_weights\}/\{target_weights\}} (\code{numeric}, optional): group-level
sums of matching columns found in \code{eligible_universe_m_d_ref};
\item \code{exp_ret_score} (\code{numeric}): \strong{weighted mean} of stock \code{exp_ret_score}
using within-group weights;
\item \code{is_eligible} (\code{integer}): set to 1 for groups retained.
}
\item \code{group_covariance_matrix} (\code{matrix} or \code{NULL}): group-by-group covariance
matrix (dimnames = groups), or \code{NULL} when \code{covariance_matrix} is \code{NULL}.
\item \code{group_liquidity_m_d_ref} (\code{data.frame} or \code{NULL}): group-level liquidity
frame containing the same columns as \code{liquidity_m_d_ref} (if provided), else \code{NULL}.
\item \code{micro_universe_m_d_ref_list} (\verb{named list}): the within-group weights used
for aggregation and for the group covariance computation.
}
}
\description{
Aggregates stock-level information into a \strong{group-level universe} (e.g., sectors)
for a single date snapshot, optionally computing:
\enumerate{
\item weighted group summaries (expected return score and liquidity metrics), and
\item a group-by-group covariance matrix using the asset-level covariance.
}

Internally, within each group the function builds a per-group weight vector
(from \code{eligible_universe_m_d_ref}). If a group's weights sum to zero, it \strong{falls
back to equal-weights} for that group and warns once. The final outputs are:
\itemize{
\item \code{group_universe_m_d_ref}: one row per group with aggregated metrics;
\item \code{group_covariance_matrix}: optional \eqn{G \times G} covariance between groups;
\item \code{group_liquidity_m_d_ref}: optional group-level liquidity frame aligned to
\code{liquidity_m_d_ref}'s columns;
\item \code{micro_universe_m_d_ref_list}: the per-group weights actually used.
}
}
\details{
\strong{Groups} are inferred from \code{eligible_universe_m_d_ref[[group_col]]} for the
provided snapshot. The function enforces:
\enumerate{
\item \code{eligible_universe_m_d_ref} contains \strong{only} eligible rows (\code{is_eligible == 1});
\item when \code{covariance_matrix} is provided, its row/column names are exactly
\code{eligible_universe_m_d_ref$tickers};
\item when \code{micro_universe_m_d_ref_list} is \code{NULL}, per-group weights are
built from \code{eligible_universe_m_d_ref$weights} and normalized within group;
if a group's sum of weights is zero, it \strong{falls back to equal-weights} with a warning.
}

\strong{Aggregation rules}
\itemize{
\item \code{exp_ret_score}: \code{stats::weighted.mean()} within each group using
the group's weights.
\item Liquidity columns: each column in \code{liquidity_m_d_ref} (except the first
three identifier columns) is aggregated by \code{stats::weighted.mean()} using
the same within-group weights.
\item Group \verb{\{*_bench_weights\}}/\code{target_weights}: computed by \code{dplyr::summarise()}
as \strong{sums across constituent stocks} for each group.
}

\strong{Group covariance (optional)}
If \code{covariance_matrix} is provided, the function calls
\code{calculate_group_covariance_matrix()} with the per-group weights list and returns
\eqn{\Sigma_G}. This is coherent with ex-ante risk decomposition when the group
weights reflect the portfolio's within-group exposures.
}
\section{Errors and Warnings}{

\itemize{
\item \strong{Errors:} missing \code{group_col}; non-matching ticker sets between
\code{eligible_universe_m_d_ref$tickers} and \code{covariance_matrix} (when provided);
invalid \code{micro_universe_m_d_ref_list} structure; presence of ineligible rows.
\item \strong{Warnings:} when a group's weights sum to zero, the function falls back
to equal-weights for that group (message: \emph{"Weights for group 'g' sum to zero. Fallback to equal weights."}).
}
}

\examples{
NULL
}
\seealso{
\code{calculate_group_covariance_matrix()}, \code{dplyr::group_by()}, \code{dplyr::summarise()},
\code{stats::weighted.mean()}, \code{purrr::map()} / \code{purrr::map_dfr()}.
}
