% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{create_port_metabacktest_config}
\alias{create_port_metabacktest_config}
\alias{create_port_metabacktest_config,port_backtest_config-method}
\title{Create Port Meta Backtest Configuration}
\usage{
create_port_metabacktest_config(meta_port_backtest_config, ...)

\S4method{create_port_metabacktest_config}{port_backtest_config}(
  meta_port_backtest_config,
  type = c("multi_port", "risk_targeted"),
  return_basis = "net",
  cost_lookback = NULL,
  cml_parameters = NULL,
  config_name = "not_identified",
  verbose = TRUE,
  ...
)
}
\arguments{
\item{meta_port_backtest_config}{A \code{port_backtest_config} describing the meta allocation. Its
\code{port_construction_method} must be one of \code{"ew"}, \code{"sw"}, \code{"rp"}, \code{"hrp"} or \code{"mvo"}, and its
\code{chosen_score_metric_and_position} must name a column of the derived \code{port_universe_m_df}.}

\item{...}{Additional arguments (not used).}

\item{return_basis}{Character, \code{"net"} (default) or \code{"raw"}. Which return basis of the base
portfolios' statistics feeds the meta universe.}

\item{cost_lookback}{\code{NULL} (default) for an expanding cost average, or a single positive whole
number of trailing months.}

\item{config_name}{Name of the backtest configuration.}

\item{verbose}{Logical, default \code{TRUE}. Whether to report the meta score's basis.}
}
\value{
A \code{port_metabacktest_config} object.
}
\description{
The \code{create_port_metabacktest_config} function creates a \code{port_metabacktest_config} object that
configures a meta-portfolio backtest: an allocation across several already-backtested portfolios,
rebalanced on a schedule, driven by those portfolios' own characteristics. It wraps a single
\code{port_backtest_config} describing the meta allocation together with the rules for reading the
base portfolios' characteristics out of a cohort. The base portfolios themselves are supplied
later, as a \code{port_backtest_cohort}, to \code{\link[=run_port_backtest]{run_port_backtest()}}.
}
\details{
The meta score is the \code{chosen_score_metric_and_position} of the wrapped config, and it must name
a column of the \linkS4class{port_universe_m_df} that \code{\link[=derive_port_universe_m_df]{derive_port_universe_m_df()}} builds from the
cohort. Because that object carries some statistics in both an ex-ante and a realized flavour,
this constructor reports which flavour the chosen score is; see \code{\link[=message_meta_score_basis]{message_meta_score_basis()}}.

See \linkS4class{port_metabacktest_config} for which slots of the wrapped config act at the meta level
and which act at the stock level.
}
\section{Functions}{
\itemize{
\item \code{create_port_metabacktest_config(port_backtest_config)}: Create a meta-backtest config from a meta-level
\code{port_backtest_config}.

}}
\examples{
\dontrun{
  # Allocate across base portfolios by their realized information ratio, signal-weighted.
  meta_config <- create_port_backtest_config(
    chosen_score_metric_and_position = c(ann_info_ratio = "long"),
    eligibility_quantile_range = c(0, 1),
    initial_buffer_period = 24,
    rebalancing_months = c(6, 12),
    selected_benchmark = "ibov",
    main_liquidity_metric = "mean_volfin_3m",
    port_construction_method = "sw",
    config_name = "meta_sw_ir"
  )

  port_meta_config <- create_port_metabacktest_config(
    meta_port_backtest_config = meta_config,
    return_basis = "net",
    config_name = "meta_sw_ir"
  )
}

}
\seealso{
\linkS4class{port_metabacktest_config}, \code{\link[=derive_port_universe_m_df]{derive_port_universe_m_df()}},
\code{\link[=create_port_backtest_config]{create_port_backtest_config()}}
}
