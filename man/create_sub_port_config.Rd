% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{create_sub_port_config}
\alias{create_sub_port_config}
\title{Create a Sub Portfolio Configuration}
\usage{
create_sub_port_config(
  port_construction_method,
  mvo_parameters = NULL,
  rp_parameters = NULL,
  hrp_parameters = NULL,
  class = "sub_port_config"
)
}
\arguments{
\item{port_construction_method}{A character string with the method used to build the
sub-portfolio. Must be one of 'ew', 'sw', 'cw', 'cs', 'rp', 'hrp' or 'mvo'.}

\item{mvo_parameters}{An object of class \code{mvo_parameters}. Only used when
\code{port_construction_method} is 'mvo'. If missing for 'mvo', a default is created.}

\item{rp_parameters}{An object of class \code{rp_parameters}. Only used when
\code{port_construction_method} is 'rp'. If missing for 'rp', a default is created.}

\item{hrp_parameters}{An object of class \code{hrp_parameters}. Only used when
\code{port_construction_method} is 'hrp'. If missing for 'hrp', a default is created.}

\item{class}{A character string naming the class to instantiate. Defaults to
\code{"sub_port_config"}. Any class extending it (e.g. \code{"mmaf_sub_port_config"}) is accepted,
which lets each layered method keep a self-documenting class name while sharing one
configuration contract.}
}
\value{
An S4 object of class \code{sub_port_config} (or of the requested subclass).
}
\description{
Constructor for a \code{sub_port_config} object, the specification of one
inner portfolio built by a layered (\verb{*af}) portfolio construction method. It says
which construction method the recursive \code{set_portfolio_weights()} call must use, and
carries the parameters of that method.
}
