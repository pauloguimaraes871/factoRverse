% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{add_risk_target_parameters}
\alias{add_risk_target_parameters}
\alias{add_risk_target_parameters,port_metabacktest_config,risk_target_parameters-method}
\alias{add_risk_target_parameters,port_metabacktest_config,missing-method}
\title{Add risk_target_parameters to a meta backtest config}
\usage{
add_risk_target_parameters(object, risk_target_params, ...)

\S4method{add_risk_target_parameters}{port_metabacktest_config,risk_target_parameters}(object, risk_target_params, ...)

\S4method{add_risk_target_parameters}{port_metabacktest_config,missing}(
  object,
  risk_target_params,
  residual_ticker,
  target,
  target_metric = c("tracking_error", "volatility"),
  p = 1,
  vol_source = c("ex_ante", "realized_rolling", "supplied"),
  vol_cov_est_method = NULL,
  vol_window = 6,
  exposure_method = c("none", "trend", "ts_adjusted", "as_is"),
  exposure_window = NULL,
  exposure_center = 1,
  exposure_sensitivity = NULL,
  exposure_bounds = c(0, 1),
  min_weight = 0,
  max_weight = 1,
  ...
)
}
\arguments{
\item{object}{An object of class \code{port_metabacktest_config}.}

\item{risk_target_params}{An object of class \code{risk_target_parameters}, or missing to build one.}

\item{...}{Additional arguments (not used).}

\item{residual_ticker, target, target_metric, p}{Passed to \code{\link[=create_risk_target_parameters]{create_risk_target_parameters()}}.}

\item{vol_source, vol_cov_est_method, vol_window}{Passed to \code{\link[=create_risk_target_parameters]{create_risk_target_parameters()}}.}

\item{exposure_method, exposure_window, exposure_center, exposure_sensitivity, exposure_bounds}{Passed to \code{\link[=create_risk_target_parameters]{create_risk_target_parameters()}}.}

\item{min_weight, max_weight}{Passed to \code{\link[=create_risk_target_parameters]{create_risk_target_parameters()}}.}
}
\value{
The updated \code{port_metabacktest_config}.
}
\description{
Either attaches an existing \code{risk_target_parameters} object or builds one from the arguments given.
Only meaningful when the configuration's \code{type} is \code{"risk_targeted"}.
}
\section{Functions}{
\itemize{
\item \code{add_risk_target_parameters(
  object = port_metabacktest_config,
  risk_target_params = risk_target_parameters
)}: Attach an existing \code{risk_target_parameters} object.

\item \code{add_risk_target_parameters(
  object = port_metabacktest_config,
  risk_target_params = missing
)}: Build a \code{risk_target_parameters} object and attach it.

}}
\seealso{
\code{\link[=create_risk_target_parameters]{create_risk_target_parameters()}}, \linkS4class{risk_target_parameters}
}
