% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-create_sb_benchmark.R
\name{create_sb_benchmark}
\alias{create_sb_benchmark}
\title{Create Signal Blending Benchmark}
\usage{
create_sb_benchmark(signal_universe_m_d_ref, signals_groups_m_d_ref = NULL)
}
\arguments{
\item{signal_universe_m_d_ref}{A data frame containing the signal universe. Must include columns:
\itemize{
\item \code{id}
\item \code{tickers}
\item \code{dates}
\item \code{top_assets} (indicating which signals are statistically significant)
}}

\item{signals_groups_m_d_ref}{An optional data frame with signal themes. Must include columns:
\itemize{
\item \code{tickers}
\item \code{theme} (the classification theme for each signal)
}}
}
\value{
A data frame with benchmark weights. Columns include:
\itemize{
\item \code{id}
\item \code{tickers}
\item \code{dates}
\item \code{individual_sb} (weight assigned to each signal)
\item \code{theme_sb} (weight assigned to each signal given their theme, if \code{signals_groups_m_d_ref} is provided)
}
}
\description{
Creates signal-blending benchmark weights for signals based on their statistical significance
}
\details{
The function calculates weights for signals based on their statistical significance.
If \code{signals_groups_m_d_ref} is provided, it also calculates theme-based weights and merges them
back into the main data frame.
}
