% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-hyper_tune.R
\name{partition_hyper_grid_domain_list}
\alias{partition_hyper_grid_domain_list}
\title{Split a Bayesian-Optimization Domain into Searched and Fixed Hyperparameters}
\usage{
partition_hyper_grid_domain_list(hyper_grid_domain_list)
}
\arguments{
\item{hyper_grid_domain_list}{A named list. Each entry is either a numeric
vector of length 2 giving \code{c(lower, upper)} bounds, or a list with
\code{distribution_choice = "constant"} and a single numeric \code{value}.}
}
\value{
A list with two elements:
\item{searched}{Named list of length-2 numeric bounds, to be passed as
\code{bounds}. Retains the order in which the hyperparameters were declared.}
\item{fixed}{Named list of single numeric values, to be spliced into the
learner call and appended to the optimal hyperparameters.}
}
\description{
Internal helper. Separates the entries of a \code{bayesian_opt} hyperparameter
domain into those the surrogate should search over and those the user pinned
to a constant.
}
\details{
A constant cannot be expressed to \code{ParBayesianOptimization} as a zero-width
bound. Candidates are rescaled by \code{(upper - lower)}, so \code{c(x, x)} produces an
all-\code{NaN} column with no error and no warning, and \code{gsPoints} defaults to
\code{pmax(100, length(bounds)^3)}, so the pinned hyperparameter still costs a
full dimension of search. Holding a hyperparameter constant therefore means
removing it from the surrogate's input space and re-inserting its value only
when the learner is called.

Constants use the same shape \code{random_search} already accepts, a list with
\code{distribution_choice = "constant"} and a single numeric \code{value}, so one
configuration reads the same way under either tuning method.
}
\keyword{internal}
