% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-summarize_posterior_draws.R
\name{summarize_posteriors_draws}
\alias{summarize_posteriors_draws}
\title{Summarize Posterior Draws for Signal Universe}
\usage{
summarize_posteriors_draws(
  brm_model,
  signal_universe_m_d_ref = NULL,
  selected_signal_themes_m_d_ref,
  model_spec_theme_level,
  compute_predictives_full = FALSE,
  n_draws_predictive = NULL
)
}
\arguments{
\item{brm_model}{A bayesian model fit with \code{brms::brm}.}

\item{signal_universe_m_d_ref}{Optional. The performance-enriched signal universe from \code{summarize_performance()}
(\code{id}, \code{tickers}, \code{dates}, plus frequentist CAPM metric columns). If \code{NULL}, the function instead returns the
raw tidy posterior-draws objects (see \verb{@return}).}

\item{selected_signal_themes_m_d_ref}{A (meta) data frame with id, tickers ("signals") and dates column contemplating all signals in \code{signals_m_df} and a "theme" column providing group membership for each signal, which is needed
for defining clusters in bayesian hierarchical model. It should contain data only for current date.}

\item{model_spec_theme_level}{A character string specifying the desired Bayesian model structure.
Options include:
\itemize{
\item \code{"random_intercept_fixed_slope"}: Includes random effects for the intercept at the theme level.
\item \code{"theme_specific_intercept_fixed_slope"}: Uses fixed intercepts for each theme.
\item \code{"theme_specific_intercept_theme_specific_slope"}: Includes fixed intercepts and slopes for each theme.
\item \code{"fixed_intercept_fixed_slope"}: Omits theme-level intercepts but includes random effects at the theme:signal level.
}}

\item{compute_predictives_full}{If TRUE, computes the full posterior predictive distribution.}

\item{n_draws_predictive}{Number of posterior draws to use for predictive computations. If NULL, all available draws are used.}
}
\value{
A named list. When \code{signal_universe_m_d_ref} is supplied: \code{signal_universe_m_d_ref} (the input,
left-joined with the posterior alpha/beta/sigma/Treynor/appraisal metrics) and \code{posterior_draws_summaries}
(a list of \code{intercept_summary}, \code{slope_summary}, \code{sd_summary}, \code{epred_summary}, \code{predicted_summary}
data frames, each a median/89\\% CI summary; the predictive ones are \code{NULL} unless \code{compute_predictives_full = TRUE}).
When \code{signal_universe_m_d_ref} is \code{NULL}: a list of the raw tidy posterior-draws data frames
(\code{tidy_posterior_draws_intercept}, \code{tidy_posterior_draws_slope}, \code{tidy_posterior_draws_sd},
\code{tidy_posterior_epred_draws}, \code{tidy_posterior_predicted_draws}).
}
\description{
This function computes various posterior summary statistics for a given set of signals, themes, and posterior draws.
It updates the \code{signal_universe_m_d_ref} data frame with posterior statistics including alphas, betas, sigmas, and other metrics.
}
\details{
The function performs the following operations for each theme:
\itemize{
\item Computes and updates the posterior overall alpha and individual alpha for each signal.
\item Computes and updates the probability of the (positive) direction for posterior alphas.
\item Computes and updates the posterior overall beta and individual beta for each signal.
\item Computes and updates the posterior sigma for each signal.
\item Computes and updates posterior metrics such as active return, tracking error, and information ratio (IR).
\item Computes and updates additional performance metrics like Appraisal Ratio (AP) and Treynor ratio.
}
}
