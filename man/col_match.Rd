% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper-col_match.R
\name{col_match}
\alias{col_match}
\title{Robust column matcher tolerant to R name-mangling}
\usage{
col_match(obj, want, ignore_case = FALSE)
}
\arguments{
\item{obj}{A matrix-, data.frame-, or xts-like object with \code{base::colnames()}.}

\item{want}{Character vector of desired column labels (may be non-syntactic).}

\item{ignore_case}{Logical; if \code{TRUE}, matching is case-insensitive. Default \code{FALSE}.}
}
\value{
Character vector of column names from \code{obj} that match \code{want},
in the same order as \code{want}. Non-matches are omitted.
}
\description{
Return the subset of \code{colnames(obj)} that correspond to \code{want}, treating
\code{"Stock C"} and \code{"Stock.C"} as equivalent. The result preserves the order of \code{want}
and drops non-matches. Column names in \code{obj} are \strong{not} modified.
}
\details{
Matching is performed on \code{base::make.names(x, unique = FALSE)}. This mirrors
how R sanitizes names (spaces/dashes → dots; leading digits prefixed with \code{X}).
If multiple columns in \code{obj} normalize to the same token, the first occurrence
is selected (per \code{base::match()} semantics).
}
\examples{
cn <- c("Stock A", "Stock.B", "X1Price")
X  <- stats::setNames(matrix(0, 2, 3), cn)
col_match(X, c("Stock A", "Stock B", "1Price"))
# -> c("Stock A", "Stock.B", "X1Price")

# Case-insensitive match:
col_match(X, c("stock a", "stock b"), ignore_case = TRUE)

}
\seealso{
base::make.names, base::match
}
\keyword{internal}
