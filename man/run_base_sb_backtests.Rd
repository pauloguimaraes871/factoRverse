% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_sb_backtest.R
\name{run_base_sb_backtests}
\alias{run_base_sb_backtests}
\title{Run Base Machine Learning Backtests}
\usage{
run_base_sb_backtests(
  features_m_df,
  target_m_df,
  target_fwd_name,
  base_sb_backtest_configs,
  verbose = TRUE,
  parallel = TRUE
)
}
\arguments{
\item{features_m_df}{A \code{meta_dataframe} object containing the features data.}

\item{target_m_df}{A \code{meta_dataframe} object containing the target data.}

\item{target_fwd_name}{A character string specifying the name of the target forward variable in \code{target_m_df}.}

\item{base_sb_backtest_configs}{A list of \code{sb_backtest_config} objects containing configurations for each base learner.}

\item{verbose}{Logical; if \code{TRUE}, prints progress messages. Default is \code{TRUE}.}

\item{parallel}{Logical; if \code{TRUE}, runs the backtests in parallel using the future framework. Default is \code{TRUE}.}
}
\value{
A list of \code{sb_backtest_results} objects, one for each base learner configuration.
}
\description{
Runs machine learning backtests for base learners using the provided configurations.
This helper function is used within \code{run_sb_backtest} but can be utilized independently if needed.
}
\examples{
# Assuming features_m_df and target_m_df are meta_dataframe objects
# and base_sb_backtest_configs is a list of sb_backtest_config objects
base_results <- run_base_sb_backtests(
  features_m_df = features_m_df,
  target_m_df = target_m_df,
  target_fwd_name = "target",
  base_sb_backtest_configs = base_sb_backtest_configs,
  verbose = TRUE,
  parallel = TRUE
)
}
