% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_heuristic_ensembles.R
\name{create_heuristic_ensembles}
\alias{create_heuristic_ensembles}
\title{Create Heuristic Ensemble of Model Configurations}
\usage{
create_heuristic_ensembles(
  ml_backtest_results_list,
  ensemble_eval_metric = "rmse",
  ensemble_huber_delta = NULL,
  ensemble_quantile_tau = NULL
)
}
\arguments{
\item{config_list}{A list of model configurations, each containing date-named lists
with numeric values (e.g., \code{list(rf_config = list("2008-05-11" = 0.5, "2008-06-11" = 0.6))}).}

\item{weights}{Optional numeric vector of weights for each configuration. The length
should match the length of \code{config_list}. Only used if \code{method = "weighted_mean"}.}

\item{method}{A character string specifying the aggregation method. Should be either
\code{"mean"} or \code{"weighted_mean"}. Defaults to \code{"mean"}.}
}
\value{
A named list where each element corresponds to a date, containing the
aggregated value (mean or weighted mean) for that date across all configurations.
}
\description{
This function creates an ensemble by combining multiple model configuration lists.
Each configuration contains a series of date-named lists with values. The function
checks if dates across configurations match and calculates the mean or weighted
mean for each date.
}
