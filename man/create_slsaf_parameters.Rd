% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{create_slsaf_parameters}
\alias{create_slsaf_parameters}
\title{Create SLSAF (Simulated Long-Short Allocation Framework) Parameters}
\usage{
create_slsaf_parameters(
  long_port_construction_method = "sw",
  long_port_config = NULL,
  bench_weight_tilt_eta = 1,
  badness_tilt_eta = 1,
  max_short_budget = NULL
)
}
\arguments{
\item{long_port_construction_method}{A character string with the method used to build
the long leg. Must be one of 'ew', 'sw', 'cw', 'cs', 'rp', 'hrp' or 'mvo'. Ignored if
\code{long_port_config} is supplied.}

\item{long_port_config}{An object of class \code{sub_port_config}. If missing, one is
created from \code{long_port_construction_method}.}

\item{bench_weight_tilt_eta}{A numeric exponent applied to the benchmark weight in the
short-leg score. Defaults to 1.}

\item{badness_tilt_eta}{A non-negative numeric exponent applied to the badness score.
Defaults to 1.}

\item{max_short_budget}{An optional numeric in (0, 1] capping the realized active
budget. Defaults to NULL, leaving it endogenous.}
}
\value{
An S4 object of class \code{slsaf_parameters}.
}
\description{
Constructor for an \code{slsaf_parameters} object, the configuration of a
long-only benchmark-relative portfolio built as a benchmark position plus a
self-financing active overlay.
}
\details{
Only the long leg is configurable. The short leg is always signal weighted on the
badness score from \code{\link{compute_short_leg_scores}}, because a risk-based method
would grant the largest underweight to the name contributing least to active risk.

On the exponents: \code{bench_weight_tilt_eta = 1} is the benchmark-proportional anchor,
where the whole available budget converts into realized underweight and every
ineligible constituent is sold in full. \code{badness_tilt_eta} moves away from that
anchor, concentrating underweight on the worst names and giving up budget in
exchange. Tracking error is otherwise governed by \code{eligibility_quantile_range} (which
sets how much benchmark mass falls outside the eligible set) and by how concentrated
the long leg is.
}
