% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-select_and_correct_signals.R
\name{select_and_correct_signals}
\alias{select_and_correct_signals}
\title{Select and Correct Signal Positions}
\usage{
select_and_correct_signals(
  signals_m_df,
  chosen_signals_and_positions,
  signal_themes_m_df = NULL,
  backtest_returns_m_xts = NULL
)
}
\arguments{
\item{signals_m_df}{A (meta) data frame with columns including "id", "tickers", "dates", and the selected signals.}

\item{chosen_signals_and_positions}{A named vector indicating signals and their corresponding positions (long or short).
For example, chosen_signals_and_positions = c(book_yield = "long", vol_36m = "short").}

\item{signal_themes_m_df}{A (meta) data frame with "id", "tickers" ("signals"), and "dates" columns, including all signals in \code{signals_m_df}, and a "theme" column providing group membership for each signal.}

\item{backtest_returns_m_xts}{A xts containing historical backtested returns named according to signals in \code{signals_m_df},}
}
\value{
A named list with three components (the last two are \code{NULL} when their
inputs are not supplied):
\itemize{
\item \code{selected_signals_corrected_positions_m_df}: The updated data frame from \code{signals_m_df} with corrected signal positions and adjusted column names.
\item \code{selected_signal_themes_m_df}: The \code{signal_themes_m_df} subset to the corrected signals (\code{NULL} if not provided).
\item \code{selected_backtest_returns_corrected_positions_m_xts}: The \code{backtest_returns_m_xts} subset to the columns matching the corrected signal positions (\code{NULL} if not provided).
}
}
\description{
This function selects signals based on a given policy, adjusts their positions according to the policy, and validates the data against backtest returns.
}
\details{
The function performs the following operations:
\itemize{
\item Extracts the chosen signals from \code{signals_m_df} and subsets the data frame to include only these signals.
\item Checks for consistency between the length of \code{chosen_signals} and \code{signal_positions} and ensures that all chosen signals have corresponding positions.
\item Adjusts the signal positions based on whether they are "short" by multiplying their values by -1.
\item Updates column names in the data frame to reflect the corrected positions of the signals.
\item Validates that all adjusted signals have corresponding columns in \code{backtest_returns_m_xts}.
}
}
