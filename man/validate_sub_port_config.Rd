% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validators.R
\name{validate_sub_port_config}
\alias{validate_sub_port_config}
\title{Validate a Sub Portfolio Configuration}
\usage{
validate_sub_port_config(object)
}
\arguments{
\item{object}{An object of class \code{sub_port_config} or of a class extending it.}
}
\value{
\code{TRUE} invisibly if the configuration is valid. Stops with an informative
message otherwise.
}
\description{
Validates a \code{sub_port_config} (or any class extending it, such as
\code{mmaf_sub_port_config}). It is the single validity contract shared by every layered
(\verb{*af}) portfolio construction method, so that a sub-portfolio is specified the same
way regardless of which method builds it.
}
\details{
Checks performed:
\itemize{
\item \code{port_construction_method} is a single, non-\code{NA} character among the methods
a sub-portfolio may use ('ew', 'sw', 'cw', 'cs', 'rp', 'hrp', 'mvo').
Layered methods are excluded on purpose: nesting one layered method inside
another is not supported.
\item the parameter object matching that method, when supplied, has the expected S4
class. Parameter objects that do not match the method are left untouched
(they are inert, not invalid).
}
}
