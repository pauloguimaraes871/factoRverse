% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_methods.R
\name{plot,meta_xts,missing-method}
\alias{plot,meta_xts,missing-method}
\title{Plot Method for meta_xts}
\usage{
\S4method{plot}{meta_xts,missing}(
  x,
  y,
  variable = NULL,
  clustering_list = NULL,
  facet_by_year = NULL,
  add_overall_means = NULL,
  vertical_lines = NULL,
  cumulative = NULL,
  plot_perf_metric = NULL,
  benchmark_returns_m_xts = NULL,
  active_returns = FALSE,
  chosen_metric = NULL,
  palette = "cyberpunk",
  ...
)
}
\arguments{
\item{x}{A \code{meta_xts} or subclass object to be plotted.}

\item{y}{Not used. Included for S4 method compatibility.}

\item{variable}{Variable to plot. If \code{NULL}, user is prompted to select from available columns.}

\item{clustering_list}{Optional named list assigning column names to cluster groups. Used to vary linetype by group.}

\item{facet_by_year}{Logical. Whether to facet plots by calendar year. If \code{NULL}, user is prompted interactively.}

\item{add_overall_means}{Logical. Whether to add dashed horizontal lines representing series-wide means.
Only applies if \code{cumulative = FALSE}. If \code{NULL}, user is prompted.}

\item{vertical_lines}{Optional vector of Dates or POSIXct timestamps at which to draw vertical dashed lines.}

\item{cumulative}{Logical. If \code{TRUE} and object is of class \code{returns_meta_xts}, plots cumulative returns (in \%). If \code{NULL}, prompts the user.}

\item{plot_perf_metric}{Logical. For \code{returns_meta_xts} only. If \code{TRUE}, plots bar chart of selected performance metric instead of time series. If \code{NULL}, user is prompted.}

\item{benchmark_returns_m_xts}{Optional \code{meta_xts} object (single-column) with benchmark returns. Required if \code{active_returns = TRUE}.}

\item{active_returns}{Logical or \code{"yes"/"no"} string. Whether to compute active returns relative to the benchmark when \code{plot_perf_metric = TRUE}.}

\item{chosen_metric}{Character. Performance metric to plot when \code{plot_perf_metric = TRUE}. Options include "CAGR", "Volatility", "Sharpe Ratio", etc. If \code{NULL}, user is prompted.}

\item{palette}{Character. Either \code{"cyberpunk"} (default; dark background,
neon series colors) or \code{"br"} (light background, brand palette).}

\item{...}{Currently unused.}
}
\value{
Invisibly returns the generated \code{ggplot} object.
}
\description{
Plots time series or performance summaries from a \code{meta_xts} (or subclass) object using \code{ggplot2}.
Offers interactive prompts for series selection, cumulative return computation, faceting, and more.
}
\details{
The function behaves differently depending on whether the object is a subclass like \code{returns_meta_xts}.
It includes:
\enumerate{
\item Interactive column selection.
\item Optional performance metric visualization for returns objects.
\item Optional cumulative return computation.
\item Option to facet plots by year.
\item Option to overlay horizontal mean lines.
\item Option to add vertical reference lines.
\item Neon color palette and dark theme styling.
}
}
\section{Requirements}{

Requires the suggested packages \code{gridExtra}, \code{scales}, \code{ggdist}, \code{ggraph},
\code{ggrepel}, \code{igraph}, and \code{RColorBrewer}; if any are missing, an informative
\code{stop()} is raised rather than a cryptic failure.
}

\section{Interactivity}{

Any of \code{variable}, \code{facet_by_year}, \code{cumulative}, \code{plot_perf_metric},
\code{active_returns}, or \code{chosen_metric} left as \code{NULL} triggers an interactive
\code{readline()} prompt. Supply all of them explicitly to use this method in
non-interactive contexts (scripts, \code{knitr}, tests).
}

\seealso{
\code{\link[=create_performance_m_df]{create_performance_m_df()}}
}
