# Check if an object is of class hyper_grid_domain

This function checks whether a given object is of class
'hyper_grid_domain'.

## Usage

``` r
is_hyper_grid_domain(x)
```

## Arguments

- x:

  The object to check.

## Value

Logical value: TRUE if the object is of class 'hyper_grid_domain', FALSE
otherwise.
