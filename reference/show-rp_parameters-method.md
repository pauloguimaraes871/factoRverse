# Show Risk-Parity Parameters

Displays the risk-parity configuration contained in a `rp_parameters`
object.

## Usage

``` r
# S4 method for class 'rp_parameters'
show(object)
```

## Arguments

- object:

  A `rp_parameters` object.
