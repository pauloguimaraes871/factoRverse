# Show Turnover Constraint Policy

Prints the contents of a `turnover_constraint_policy` object, including
the quantile range buffer and the turnover cap rules.

## Usage

``` r
# S4 method for class 'turnover_constraint_policy'
show(object)
```

## Arguments

- object:

  A `turnover_constraint_policy` object.
