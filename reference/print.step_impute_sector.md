# Print Method for step_impute_sector

Prints a concise summary of the `step_impute_sector` step, including
selected columns, the sector column, and the imputation method.

## Usage

``` r
# S3 method for class 'step_impute_sector'
print(x, width = max(20, options()$width - 30), ...)
```

## Arguments

- x:

  A `step_impute_sector` object.

- width:

  An integer specifying the desired display width. Defaults to a dynamic
  value based on console width.

- ...:

  Additional arguments (not used).

## Value

The original `step_impute_sector` object, invisibly.
