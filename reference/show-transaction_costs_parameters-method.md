# Show Transaction Cost Parameters

Prints the contents of a `transaction_costs_parameters` object,
including direct transaction cost, strategy AUM, alpha, and lambda.

## Usage

``` r
# S4 method for class 'transaction_costs_parameters'
show(object)
```

## Arguments

- object:

  A `transaction_costs_parameters` object.
