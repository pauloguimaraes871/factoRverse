# Define the is_hyperparameter_tuning_strategy function

Function to check if an object is of class
`hyperparameter_tuning_strategy`.

## Usage

``` r
is_tuning_strategy(x)
```

## Arguments

- x:

  An object to check.

## Value

A logical value indicating whether the object is of class
`hyperparameter_tuning_strategy`.
