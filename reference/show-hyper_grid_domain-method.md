# Print method for hyper_grid_domain

This method prints the contents of a `hyper_grid_domain` object in a
user-friendly format.

## Usage

``` r
# S4 method for class 'hyper_grid_domain'
show(object)
```

## Arguments

- object:

  A `hyper_grid_domain` object to be printed.
