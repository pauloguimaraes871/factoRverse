# as.list Method for liquidity_constraint_policy S4 Class

Converts a liquidity_constraint_policy S4 object to a list with elements
`liquidity_floor_rule` and `liquidity_cap_rules`.

## Usage

``` r
# S4 method for class 'liquidity_constraint_policy'
as.list(x, ...)
```

## Arguments

- x:

  A liquidity_constraint_policy S4 object.

- ...:

  Additional arguments (unused).

## Value

A list with elements `liquidity_floor_rule` and `liquidity_cap_rules`.
