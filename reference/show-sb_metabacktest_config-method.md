# Show Method for sb_metabacktest_config Class

Displays detailed information about each configuration in the
`sb_metabacktest_config` object.

## Usage

``` r
# S4 method for class 'sb_metabacktest_config'
show(object)
```

## Arguments

- object:

  An `sb_metabacktest_config` object.

## Value

Invisibly returns `NULL`. This function is called for its side effect of
displaying information.
