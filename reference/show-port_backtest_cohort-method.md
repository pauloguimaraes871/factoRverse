# Show Method for port_backtest_cohort Class

Displays a detailed summary of a `port_backtest_cohort` object. It
focuses on the configuration settings contained in the common
port_backtest_workflow slot (e.g., selected_benchmark,
port_construction_method, object names, dates, and more).

## Usage

``` r
# S4 method for class 'port_backtest_cohort'
show(object)
```

## Arguments

- object:

  An object of class `port_backtest_cohort`.
