# Show Port Backtest Config

Prints the contents of a `port_backtest_config` object, detailing its
various parameters and configurations for stock-level portfolio
backtesting.

## Usage

``` r
# S4 method for class 'port_backtest_config'
show(object)
```

## Arguments

- object:

  A `port_backtest_config` object to be displayed.
