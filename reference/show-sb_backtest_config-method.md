# Show SB Backtest Config

Prints the contents of an `sb_backtest_config` object, detailing the
various parameters and their configurations.

## Usage

``` r
# S4 method for class 'sb_backtest_config'
show(object)
```

## Arguments

- object:

  An `sb_backtest_config` object to be displayed.
