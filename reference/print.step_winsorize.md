# Print Method for step_winsorize

Print method for a `step_winsorize` object.

## Usage

``` r
# S3 method for class 'step_winsorize'
print(x, width = max(20, options()$width - 30), ...)
```

## Arguments

- x:

  A `step_winsorize` object.

- width:

  Width of the output. Default adjusts to console width.

- ...:

  Additional arguments (not used).

## Value

The original `step_winsorize` object, invisibly.
