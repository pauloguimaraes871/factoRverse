# Show Method for sb_metabacktest_results Class

Displays the contents of an `sb_metabacktest_results` object, including
consolidated and time series evaluation metrics.

## Usage

``` r
# S4 method for class 'sb_metabacktest_results'
show(object)
```

## Arguments

- object:

  An object of class `sb_metabacktest_results`.
