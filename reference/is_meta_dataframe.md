# Check if an object is a meta_dataframe

Check if an object is a meta_dataframe

## Usage

``` r
is_meta_dataframe(x)
```

## Arguments

- x:

  The object to check.

## Value

TRUE if x is of class "meta_dataframe", FALSE otherwise.
