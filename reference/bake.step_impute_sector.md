# Apply imputation during baking

Apply imputation during baking

## Usage

``` r
# S3 method for class 'step_impute_sector'
bake(object, new_data, ...)
```

## Arguments

- object:

  A `step_impute_sector` object.

- new_data:

  A data frame of new data to which the imputation will be applied.

- ...:

  Additional arguments passed to methods (not used).
