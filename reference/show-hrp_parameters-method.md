# Show HRP Parameters

Displays the hierarchical risk parity configuration contained in a
`hrp_parameters` object.

## Usage

``` r
# S4 method for class 'hrp_parameters'
show(object)
```

## Arguments

- object:

  A `hrp_parameters` object.
