# as.list Method for turnover_constraint_policy S4 Class

Converts a turnover_constraint_policy S4 object to a list with elements
`quantile_range_buffer` and `turnover_cap_rules`.

## Usage

``` r
# S4 method for class 'turnover_constraint_policy'
as.list(x, ...)
```

## Arguments

- x:

  A turnover_constraint_policy S4 object.

- ...:

  Additional arguments (unused).

## Value

A list with elements `quantile_range_buffer` and `turnover_cap_rules`.
