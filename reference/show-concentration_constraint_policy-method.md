# Show Concentration Constraint Policy

Prints the contents of a `concentration_constraint_policy` object,
detailing the benchmark, the maximum absolute active weight for
individual assets, and the maximum absolute active group weights.

## Usage

``` r
# S4 method for class 'concentration_constraint_policy'
show(object)
```

## Arguments

- object:

  A `concentration_constraint_policy` object.
