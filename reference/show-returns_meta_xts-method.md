# Show method for returns_meta_xts

Shows a summary of the `returns_meta_xts` object, including metadata and
the first few rows of the underlying `xts` data.

## Usage

``` r
# S4 method for class 'returns_meta_xts'
show(object)
```

## Arguments

- object:

  An object of class `returns_meta_xts`.

## Value

Returns the object invisibly after printing its summary.
