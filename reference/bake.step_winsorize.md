# Apply winsorization during baking

Apply winsorization during baking

## Usage

``` r
# S3 method for class 'step_winsorize'
bake(object, new_data, ...)
```

## Arguments

- object:

  A trained `step_winsorize` object.

- new_data:

  A data frame to which the winsorization will be applied.

- ...:

  Additional arguments passed to methods (not used).
